# Быстрый деплой бота на Railway

## Шаг 1: Создать новый сервис

1. Railway Dashboard → ваш проект → **"+ New"** → **"Empty Service"**
2. Назовите: `telegram-bot`

## Шаг 2: Подключить репозиторий

1. Settings → Source → **"Connect GitHub Repo"**
2. Выберите ваш репозиторий

## Шаг 3: Установить переменные окружения

Settings → Variables → добавьте:

```
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_ADMIN_ID=ваш_id
TELEGRAM_GROUP_ID=id_группы
TELEGRAM_TOPIC_ID=id_топика  # опционально
```

## Шаг 4: Установить команду запуска

### Способ 1: Использовать Root Directory (рекомендуется)

1. Settings → Deploy
2. Найдите поле **"Root Directory"** (или **"Service Root"**)
3. Установите: `telegram_bot`
4. Сохраните

Railway будет использовать `telegram_bot/railway.json` с правильной командой запуска.

### Способ 2: Если Root Directory недоступен

1. Временно переименуйте `railway.json` в корне в `railway.json.backup`
2. Закоммитьте и запушьте изменения
3. Settings → Deploy → Start Command (теперь поле доступно):
   ```
   python telegram_bot/run_bot.py
   ```
4. Верните `railway.json` обратно
5. Для веб-сервиса установите команду вручную через UI

**Подробнее:** см. `RAILWAY_FIX_START_COMMAND.md`

## Готово! 🎉

Railway автоматически:
- Соберет и задеплоит бота
- Запустит его постоянно
- Будет перезапускать при сбоях

## Проверка

1. **⚠️ ВАЖНО: Убедитесь, что смотрите логи сервиса telegram-bot, а не веб-сервиса!**

2. **Проверьте логи в Railway Dashboard → сервис telegram-bot:**
   - Должна быть строка: `Бот запущен и готов к работе`
   - Должна быть информация: `Бот: @your_bot_username (ID: ...)`
   - **НЕ должно быть строк про gunicorn!** (это логи веб-сервиса)

3. **Отправьте `/start` боту в личке:**
   - Бот должен ответить приветственным сообщением

4. **Отправьте тестовое сообщение с `#BUG` в группу:**
   - Репорт должен прийти в личку

## Важно

- Бот будет работать 24/7
- Логи доступны в Railway Dashboard
- При обновлении кода бот автоматически перезапустится
- **Создайте Volume для базы данных** (см. RAILWAY_DEPLOY.md)

## Если бот не отвечает

См. `TROUBLESHOOTING.md` для решения проблем.