# Быстрый старт Telegram-бота

## Шаг 1: Получить токен бота

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Узнать свой Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш ID (число, например `123456789`)

## Шаг 3: Установить переменные окружения

### Windows (PowerShell):
```powershell
$env:TELEGRAM_BOT_TOKEN="ваш_токен_от_BotFather"
$env:TELEGRAM_ADMIN_ID="ваш_telegram_id"
$env:TELEGRAM_GROUP_ID="id_группы_тестировщиков"
$env:TELEGRAM_TOPIC_ID="id_топика"  # Опционально: для отправки ответов в конкретный топик
```

### Linux/Mac:
```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_от_BotFather"
export TELEGRAM_ADMIN_ID="ваш_telegram_id"
export TELEGRAM_GROUP_ID="id_группы_тестировщиков"
export TELEGRAM_TOPIC_ID="id_топика"  # Опционально: для отправки ответов в конкретный топик
```

### Или создать файл `.env`:
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_ADMIN_ID=ваш_telegram_id
TELEGRAM_GROUP_ID=id_группы_тестировщиков
TELEGRAM_TOPIC_ID=id_топика  # Опционально: для отправки ответов в конкретный топик
```

**Важно:** 
- `TELEGRAM_GROUP_ID` - это ID супергруппы, где работают тестировщики. Чтобы узнать ID группы:
  1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в группу
  2. Отправьте любое сообщение в группу
  3. Бот покажет ID группы (отрицательное число, например `-1001234567890`)

- `TELEGRAM_TOPIC_ID` (опционально) - это ID топика, куда бот будет отправлять ответы при изменении статуса. Чтобы узнать ID топика:
  1. Отправьте `/getid` в нужном топике группы
  2. Бот покажет ID топика (положительное число, например `12345`)
  3. Если не указан, ответы будут отправляться в основной чат группы

## Шаг 4: Установить зависимости

```bash
pip install -r requirements.txt
```

## Шаг 5: Добавить бота в группу

1. Найдите вашего бота в Telegram (по имени, которое вы дали в BotFather)
2. Добавьте бота в группу тестировщиков
3. Дайте боту права администратора (или убедитесь, что он может читать сообщения)

## Шаг 6: Запустить бота

```bash
python -m telegram_bot.bot
```

Или:

```bash
python telegram_bot/run_bot.py
```

## Шаг 7: Протестировать

Отправьте в группу сообщение с тегом:

```
#BUG Тестовая ошибка для проверки бота
```

Бот должен:
1. Обнаружить тег
2. Отправить репорт вам в личку
3. Показать кнопки для управления статусом

## Готово! 🎉

Бот работает и отслеживает репорты в группе.

## Полезные команды

- `/start` - информация о боте
- `/stats` - статистика репортов (только для админа)

## Устранение проблем

**Бот не отвечает:**
- Проверьте токен бота
- Убедитесь, что бот добавлен в группу
- Проверьте логи на ошибки

**Репорты не приходят:**
- Проверьте, что `TELEGRAM_ADMIN_ID` правильный
- Убедитесь, что сообщения содержат теги (#BUG, #UIFIX, #FEATURE)
- Проверьте, что бот может отправлять вам сообщения (напишите боту `/start`)