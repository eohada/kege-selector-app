# Инструкция по деплою staging-окружения на Render

Это руководство поможет развернуть копию сайта на Render для тестирования, не затрагивая основной сайт.

## 📋 Предварительные требования

1. **Аккаунт на Render.com** - зарегистрируйся на [render.com](https://render.com) (можно через GitHub)
2. **GitHub репозиторий** - проект должен быть в GitHub (можно приватный)
3. **Локальная копия БД** - убедись, что у тебя есть актуальный дамп `data/keg_tasks.db`

## 🚀 Шаг 1: Подготовка проекта

### 1.1. Проверь файлы конфигурации

Убедись, что в проекте есть:
- ✅ `render.yaml` - конфигурация для автоматического деплоя
- ✅ `requirements.txt` - обновлён с gunicorn и psycopg2-binary
- ✅ `scripts/init_staging_db.py` - скрипт для инициализации БД

### 1.2. Закоммить изменения

```bash
git add render.yaml requirements.txt scripts/init_staging_db.py
git commit -m "Add Render deployment configuration"
git push origin main
```

## 🔧 Шаг 2: Создание сервисов на Render

### 2.1. Подключение GitHub репозитория

1. Зайди на [dashboard.render.com](https://dashboard.render.com)
2. Нажми **"New +"** → **"Blueprint"**
3. Выбери **"Create a new Blueprint"**
4. Подключи свой GitHub аккаунт и выбери репозиторий
5. Render автоматически обнаружит `render.yaml` и предложит создать сервисы

### 2.2. Автоматическое создание через Blueprint

Render создаст:
- **Web Service** (`kege-selector-staging`) - Flask приложение
- **PostgreSQL Database** (`kege-db-staging`) - база данных

### 2.3. Ручное создание (если Blueprint не сработал)

#### Создание PostgreSQL Database:

1. **New +** → **PostgreSQL**
2. Настройки:
   - **Name**: `kege-db-staging`
   - **Database**: `kege_staging`
   - **User**: `kege_user`
   - **Plan**: **Free** (90 дней, 256 MB)
3. Нажми **Create Database**
4. Скопируй **Internal Database URL** (понадобится позже)

#### Создание Web Service:

1. **New +** → **Web Service**
2. Подключи GitHub репозиторий
3. Настройки:
   - **Name**: `kege-selector-staging`
   - **Environment**: **Python 3**
   - **Build Command**: 
     ```bash
     pip install -r requirements.txt && playwright install chromium
     ```
   - **Start Command**:
     ```bash
     gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120
     ```
   - **Plan**: **Free**

4. **Environment Variables**:
   - `PYTHON_VERSION` = `3.11.0`
   - `SECRET_KEY` = (нажми "Generate" для автоматической генерации)
   - `ADMIN_SECRET` = (нажми "Generate" для автоматической генерации, или создай свой сложный ключ)
   - `FLASK_ENV` = `production`
   - `DATABASE_URL` = (вставь Internal Database URL из PostgreSQL)
   
   **Важно:** Сохрани `ADMIN_SECRET` в безопасном месте! Он нужен для доступа к админ-панели логов.

5. Нажми **Create Web Service**

## 🗄️ Шаг 3: Инициализация базы данных

### 3.1. Через Render Shell (рекомендуется)

1. В Dashboard Render открой свой **Web Service**
2. Перейди на вкладку **"Shell"**
3. Выполни команды:

```bash
# Установка зависимостей (если нужно)
pip install psycopg2-binary

# Запуск скрипта инициализации
python scripts/init_staging_db.py
```

### 3.2. Через локальный скрипт (альтернатива)

Если у тебя есть доступ к DATABASE_URL:

```bash
# Установи переменную окружения
export DATABASE_URL="postgresql://user:pass@host:5432/dbname"

# Запусти скрипт
python scripts/init_staging_db.py
```

## ✅ Шаг 4: Проверка работы

1. Открой URL твоего Web Service (например, `https://kege-selector-staging.onrender.com`)
2. Проверь, что сайт загружается
3. Проверь основные функции:
   - Вход/регистрация (если есть)
   - Просмотр учеников
   - Генератор заданий
   - Расписание

## 🔄 Шаг 5: Обновление staging из production

Когда нужно обновить staging данными из production:

### Вариант 1: Через скрипт (рекомендуется)

```bash
# На локальной машине с production БД
export DATABASE_URL="postgresql://staging-connection-string"
python scripts/init_staging_db.py
```

### Вариант 2: Через Render Shell

1. Открой Shell в Render Dashboard
2. Выполни команду инициализации заново

## 🛡️ Шаг 6: Безопасность

### Доступ к админ-панели логов

После деплоя получи `ADMIN_SECRET` из переменных окружения Render:

1. Зайди в Dashboard → Web Service → Environment
2. Найди переменную `ADMIN_SECRET` и скопируй её значение
3. Открой админ-панель по адресу:
   ```
   https://kege-selector-staging.onrender.com/admin-audit?secret=ТВОЙ_ADMIN_SECRET
   ```

**Важно:** 
- Сохрани `ADMIN_SECRET` в безопасном месте
- Не передавай его тестировщикам
- Используй сложный случайный ключ для production

### Ограничение доступа (опционально)

Если хочешь ограничить доступ только для тестировщиков:

1. В настройках Web Service добавь переменную:
   - `BASIC_AUTH_USER` = `testuser`
   - `BASIC_AUTH_PASS` = `testpass123`

2. Добавь middleware в `app.py` для Basic Auth (если нужно)

### Важные моменты:

- ⚠️ **Не храни production секреты** в staging окружении
- ⚠️ **Staging БД может быть удалена** через 90 дней на free плане
- ⚠️ **Web Service засыпает** после 15 минут неактивности (free план)
- ✅ **Все изменения изолированы** от production
- ✅ **Логи действий тестировщиков** доступны через админ-панель

## 📝 Полезные команды

### Просмотр логов

```bash
# В Render Dashboard → Web Service → Logs
```

### Перезапуск сервиса

```bash
# В Render Dashboard → Web Service → Manual Deploy → Clear build cache & deploy
```

### Сброс БД

```bash
# В Render Shell
python scripts/init_staging_db.py
```

## 🐛 Решение проблем

### Проблема: "Database connection failed"

- Проверь, что `DATABASE_URL` правильно установлен
- Убедись, что PostgreSQL сервис запущен
- Проверь, что Internal Database URL используется (не External)

### Проблема: "Playwright browsers not found"

- Убедись, что в Build Command есть `playwright install chromium`
- Проверь логи сборки на наличие ошибок

### Проблема: "Application crashes on startup"

- Проверь логи в Render Dashboard
- Убедись, что все зависимости установлены
- Проверь, что `SECRET_KEY` установлен

### Проблема: "Static files not loading"

- Убедись, что папка `static/` закоммичена в Git
- Проверь пути в шаблонах (используй `url_for('static', ...)`)

## 📞 Поддержка

Если что-то не работает:
1. Проверь логи в Render Dashboard
2. Проверь документацию Render: [render.com/docs](https://render.com/docs)
3. Создай issue в репозитории

---

**Готово!** Теперь у тебя есть изолированная staging-копия для тестировщиков. 🎉

