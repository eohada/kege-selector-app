# Инструкция: Размещение проекта на GitHub

## 📋 Предварительные требования

- ✅ Аккаунт на GitHub (если нет - зарегистрируйся на [github.com](https://github.com))
- ✅ Git установлен на компьютере
- ✅ Проект готов к загрузке

## 🚀 Шаг 1: Проверка Git

Открой терминал в папке проекта и проверь, что Git инициализирован:

```bash
cd D:\VSCode\kege_selector_app
git status
```

Если видишь ошибку "not a git repository", значит Git еще не инициализирован. Переходи к Шагу 2.

Если видишь список файлов - Git уже инициализирован, переходи к Шагу 3.

## 🔧 Шаг 2: Инициализация Git (если нужно)

Если Git не инициализирован, выполни:

```bash
git init
```

## 📝 Шаг 3: Создание .gitignore

Убедись, что есть файл `.gitignore` (он должен быть в проекте). Если его нет, создай:

```bash
# Проверь наличие файла
dir .gitignore
```

Файл `.gitignore` должен исключать:
- `venv/` - виртуальное окружение
- `__pycache__/` - кэш Python
- `*.db` - базы данных
- `.env` - переменные окружения
- И другие временные файлы

## 📦 Шаг 4: Добавление файлов в Git

Добавь все файлы проекта:

```bash
git add .
```

Проверь, что добавилось:

```bash
git status
```

## 💾 Шаг 5: Первый коммит

Создай первый коммит:

```bash
git commit -m "Initial commit: KEGE selector app with audit logging"
```

## 🌐 Шаг 6: Создание репозитория на GitHub

1. Зайди на [github.com](https://github.com) и войди в аккаунт
2. Нажми кнопку **"+"** в правом верхнем углу
3. Выбери **"New repository"**
4. Заполни форму:
   - **Repository name**: `kege-selector-app` (или любое другое имя)
   - **Description**: "Генератор заданий КЕГЭ с системой логирования"
   - **Visibility**: 
     - **Public** - если хочешь открытый репозиторий
     - **Private** - если хочешь приватный (рекомендуется для production)
   - **НЕ ставь галочки** на "Add a README file", "Add .gitignore", "Choose a license" (у тебя уже есть файлы)
5. Нажми **"Create repository"**

## 🔗 Шаг 7: Подключение локального репозитория к GitHub

После создания репозитория GitHub покажет инструкции. Выполни команды:

```bash
# Добавь удаленный репозиторий (замени USERNAME и REPO_NAME на свои)
git remote add origin https://github.com/USERNAME/REPO_NAME.git

# Переименуй ветку в main (если нужно)
git branch -M main

# Загрузи код на GitHub
git push -u origin main
```

**Пример:**
Если твой username `john` и репозиторий `kege-selector-app`, команда будет:
```bash
git remote add origin https://github.com/john/kege-selector-app.git
git branch -M main
git push -u origin main
```

## 🔐 Шаг 8: Авторизация (если требуется)

При первом `git push` GitHub может запросить авторизацию:

### Вариант A: Personal Access Token (рекомендуется)

1. Зайди в GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажми **"Generate new token (classic)"**
3. Назови токен (например, "My Computer")
4. Выбери срок действия (например, 90 days)
5. Отметь права: **repo** (полный доступ к репозиториям)
6. Нажми **"Generate token"**
7. **Скопируй токен** (он показывается только один раз!)
8. При запросе пароля в терминале:
   - Username: твой GitHub username
   - Password: вставь токен (не пароль от аккаунта!)

### Вариант B: GitHub CLI

Установи GitHub CLI и авторизуйся:

```bash
# Установка (если нет)
winget install GitHub.cli

# Авторизация
gh auth login
```

### Вариант C: SSH ключи (для продвинутых)

Если настроены SSH ключи, используй SSH URL вместо HTTPS:
```bash
git remote set-url origin git@github.com:USERNAME/REPO_NAME.git
```

## ✅ Шаг 9: Проверка

После успешной загрузки:

1. Обнови страницу репозитория на GitHub
2. Убедись, что все файлы загружены
3. Проверь, что `.gitignore` работает (файлы из `venv/` и `*.db` не должны быть видны)

## 🔄 Шаг 10: Последующие обновления

Когда делаешь изменения в проекте:

```bash
# Проверь изменения
git status

# Добавь измененные файлы
git add .

# Создай коммит
git commit -m "Описание изменений"

# Загрузи на GitHub
git push
```

## 📋 Чеклист

- [ ] Git инициализирован в проекте
- [ ] Создан `.gitignore` файл
- [ ] Все файлы добавлены в Git (`git add .`)
- [ ] Создан первый коммит (`git commit`)
- [ ] Создан репозиторий на GitHub
- [ ] Локальный репозиторий подключен к GitHub (`git remote add origin`)
- [ ] Код загружен на GitHub (`git push`)
- [ ] Файлы видны на GitHub

## ⚠️ Важные замечания

### Что НЕ нужно загружать на GitHub:

- ❌ `venv/` - виртуальное окружение (должно быть в `.gitignore`)
- ❌ `*.db` - базы данных (должны быть в `.gitignore`)
- ❌ `.env` - переменные окружения с секретами
- ❌ `__pycache__/` - кэш Python
- ❌ Личные данные и пароли

### Что нужно загрузить:

- ✅ Весь исходный код (`.py` файлы)
- ✅ Шаблоны (`.html` файлы)
- ✅ Статические файлы (`.css`, `.js`)
- ✅ Конфигурационные файлы (`requirements.txt`, `render.yaml`)
- ✅ Документация (`.md` файлы)
- ✅ `.gitignore`

## 🆘 Решение проблем

### Проблема: "remote origin already exists"

**Решение:**
```bash
git remote remove origin
git remote add origin https://github.com/USERNAME/REPO_NAME.git
```

### Проблема: "failed to push some refs"

**Решение:**
```bash
# Получи изменения с GitHub (если там что-то есть)
git pull origin main --allow-unrelated-histories

# Затем попробуй снова
git push -u origin main
```

### Проблема: "authentication failed"

**Решение:**
- Используй Personal Access Token вместо пароля
- Или настрой SSH ключи

### Проблема: Слишком большой файл

Если какой-то файл слишком большой (например, база данных):

```bash
# Удали файл из истории Git
git rm --cached data/keg_tasks.db

# Добавь в .gitignore
echo "data/*.db" >> .gitignore

# Закоммить изменения
git add .gitignore
git commit -m "Remove database from git"
git push
```

## 📚 Полезные команды

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log

# Посмотреть подключенные репозитории
git remote -v

# Изменить URL репозитория
git remote set-url origin НОВЫЙ_URL

# Отменить последний коммит (если еще не запушен)
git reset --soft HEAD~1
```

---

**Готово!** Теперь твой проект на GitHub и готов к подключению к Render! 🎉

