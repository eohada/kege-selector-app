# История изменений проекта BooStudy (Changelog & Plan)

Каждая запись должна снабжаться датой, временем (местным) и списком изменений.

---

### [14.07.2026 16:36]

#### Выполненные задачи:

1. **Раздел 1: Вход, Промокоды и Доступ**
   - Добавлено примечание (поле `note`) к модели `PromoCode` в базе данных.
   - Ограничена максимальная ширина (`max-width: 480px`) для страниц входа, запроса сброса пароля и формы ввода нового пароля, чтобы формы не растягивались на весь экран.
   - Создана страница отладки `/debug/last-email` (свободный доступ без авторизации) для просмотра последнего отправленного письма восстановления пароля прямо в браузере.
   - Исправлена контрастность элементов светлой темы в дашборде (сделан читаемый текст и карточки статистики).

2. **Раздел 2: Перенос и доработка Админки**
   - **Локальный перенос:** Отключен принудительный редирект на удаленную админку (`_redirect_admin_ui_to_remote` возвращает `None`), маршрут `/admin` перенаправляет на локальное управление пользователями.
   - **Доступы:** Ограничен вход в админку. Доступ имеют только роли Создатель (`creator`), Администратор (`admin`) и Главный администратор (`chief_admin`).
   - **Навигация:** Создан общий шаблон вкладок [templates/admin/_admin_nav.html](file:///e:/projects/kege_selector_app_current/templates/admin/_admin_nav.html) для быстрого переключения между всеми 10 разделами локальной админки. Сделана горизонтальная прокрутка (`overflow-x: auto`) без переноса кнопок.
   - **Починка роутов:** Устранены BuildError-ошибки при вызовах `admin.maintenance_page` и `admin.debug_export`.
   - **Починка графа связей:** Контейнер графа связей на вкладке «Связи (граф)» обернут в адаптивные CSS-классы `.graph-shell` и `.graph-stage`, благодаря чему интерактивная SVG-схема связей инициализируется и отображается корректно.
   - **Очистка фильтров:** На странице пользователей панель фильтрации сжата, кнопки перенесены ближе к селекторам, кнопка применения перекрашена в акцентный фиолетовый цвет.
   - **Двухколоночный редактор:** Форма создания/редактирования пользователя в [templates/admin_user_edit.html](file:///e:/projects/kege_selector_app_current/templates/admin_user_edit.html) перестроена в две колонки (слева — профиль и пароль, справа — доступы и связи) для устранения длинного вертикального полотна.
   - **Локальные логи:** Из роута `/admin-audit` удален редирект в удаленную админку, теперь логи отображаются на локальной странице.

#### Планируемые задачи (В очереди):
- [ ] Интеграция функционала удаленной админки (создание пакетов, синхронизация прототипов, эталоны) при необходимости.
- [ ] Оптимизация и тестирование системы ролей и прав.

---

### [14.07.2026 17:37]

#### Выполненные задачи:
1. **Анализ готовности к тестированию:**
   - Проведен полный технический аудит кодовой базы по 7 зонам тестирования (авторизация, биллинг, админка, курсы/уроки, генераторы задач, песочница запуска кода, Telegram-интеграция).
   - Выявлена критическая проблема: отсутствует бэкенд-поддержка запуска C++ в песочнице (поддерживается только Python).
   - Выявлены слепые зоны тестирования: файловый менеджер ученика (Workspace), интерактивный холст рисования (Canvas), нагрузка на сервер при выполнении кода, режим обслуживания.
   - Сформирован подробный отчет для пользователя с оценкой общей готовности (~85%).

#### Планируемые задачи (В очереди):
- [ ] Обсудить с пользователем необходимость добавления компиляции/запуска C++ кода в песочницу.
- [ ] Проверить сохранение таймера генератора на стороне бэкенда при перезагрузке страницы.
- [ ] Провести ручное тестирование и аудит ролей/доступов.

---

### [14.07.2026 17:53]

#### Выполненные задачи:
1. **Реструктуризация графа Obsidian Knowledge Base:**
   - Убраны все перекрёстные и обратные ссылки из листовых нод (`Database_Schema`, `Pages_and_Routes`, `Sandbox`, `Billing`) — они не должны ссылаться назад наверх.
   - Перестроены хабы: `Project_Map.md` теперь является единым центральным узлом с 4 ветками (Разработка, Тестирование, Текущая работа, История).
   - `Tech_Stack.md` ссылается только вниз: `Database_Schema` → `Pages_and_Routes` → `Core_Functions`.
   - `Core_Functions.md` расширен: добавлены блоки по RBAC-ролям, учебному процессу, Celery-задачам и ИИ-ассистенту (GigaChat). Ссылается вниз на `Sandbox`, `Billing`, `Telegram_Integration`.
   - `Test_Plan_7_Zones.md` ссылается вниз: `Test_Readiness` → `E2E_Tests` → `Bug_Tracker`.
   - `Welcome.md` обновлён — стал точкой входа для пользователей и агентов.
   
2. **Новые файлы в Obsidian Vault:**
   - `Features/Telegram_Integration.md` — полное описание интеграции с ботом, типов уведомлений, привязки аккаунтов и Telegram Mini App.
   - `Tasks/Current_Work.md` — хаб текущего спринта (ссылается на Todo и Session_Log).
   - `Tasks/Session_Log.md` — хронологический лог рабочих сессий для синхронизации между агентами и аккаунтами.

3. **Жёсткое правило задокументировано:**
   - В `AGENTS.md` прописано правило: любой, даже самый мелкий чих в коде — немедленно в `CHANGELOG.md` проекта и `Changelog.md` в Obsidian Vault.

- [ ] Взять в работу задачу из текущего спринта (таймер генератора при F5).
- [ ] Обсудить добавление поддержки C++ в песочницу.
- [ ] Настроить тестовые токены Telegram-бота для тестировщиков.

---

### [14.07.2026 19:25]

#### Выполненные задачи:
1. **Анализ и проверка сохранения таймера при обновлении страницы (F5):**
   - Проверена логика работы таймера сдачи тестов (`Submission`).
   - Время старта (`started_at`) сохраняется на сервере в PostgreSQL при начале выполнения (роут `/submissions/<submission_id>/start`).
   - На фронтенде (`templates/submission_view.html`) таймер пересчитывается каждую секунду как разница между окончанием лимита (`limitEndMs = started_at + time_limit_minutes`) и текущим временем клиента (`Date.now()`).
   - Перезагрузка страницы по F5 (или закрытие вкладки) не сбрасывает таймер, отсчет продолжается корректно.
2. **Ведение планов и синхронизация:**
   - Обновлен файл `Tasks/Todo.md` и добавлен лог сессии в `Tasks/Session_Log.md` в Obsidian.

#### Планируемые задачи (В очереди):
- [ ] Обсудить поддержку C++ в песочнице (требуется ли она раннеру на бэкенде).
- [ ] Настроить тестовые токены Telegram-ботов для QA-тестирования.
- [ ] Протестировать права доступа/роли (`admin`, `creator`, `tutor`, `student`).

---

### [14.07.2026 19:37]

#### Выполненные задачи:
1. **Реструктуризация графа Obsidian под идеальное дерево:**
   - Избавление от "паутины": удалены абсолютно все перекрестные Wikilinks (`[[ ]]`) из `Welcome.md`, `Project_Map.md`, `Tasks/Current_Work.md`, `Obsidian_Guidelines.md`.
   - Настроена строгая иерархическая топология: `Welcome` -> `Project_Map` -> Хабы (`Tech_Stack`, `Test_Plan_7_Zones`, `Current_Work`, `Changelog`, `Obsidian_Guidelines`) -> Листовые файлы без обратных и перекрестных ссылок.
   - **Устранение скрытых связей**: выявлено, что относительные Markdown-ссылки `[Текст](относительный_путь.md)` в Obsidian Graph View всё равно индексируются как связи и связывают хабы (`Current_Work` -> `Tech_Stack` и др.), ломая дерево. Они заменены на абсолютные ссылки с протоколом `file:///` для полного разрыва паразитных ребер.
   - В `Obsidian_Guidelines.md` добавлено подробное руководство по визуальной настройке графа в Obsidian (Groups, Forces, Center force, Repulsion, Link distance) для красивого цветного разделения веток.
---

### [14.07.2026 19:47]

#### Выполненные задачи:
1. **Расширение базы знаний Obsidian (документирование всех модулей):**
   - Созданы **13 новых подробных спецификаций** в папке `Features/` базы знаний, полностью описывающих каждую деталь, модели данных, бизнес-логику и роуты абсолютно всех модулей платформы (папка `app/`):
     - `Analytics_and_Rating.md` — Логика MMR (Matchmaking Rating), статистика успеваемости и аналитические логи.
     - `Course_Builder.md` — Конструктор курсов, учебных модулей и уроков.
     - `Lessons_and_Sockets.md` — Интерактивные уроки, комнаты Socket.IO, трансляция изменений в реальном времени и чат.
     - `Task_Workspace.md` — IDE в браузере, автосохранение черновиков на сервере/клиенте, CodeMirror и canvas.
     - `Theory_and_Library.md` — Библиотека знаний, рендеринг LaTeX/Markdown и KaTeX.
     - `AI_Trainer.md` — Интеграция с GigaChat SDK, автодиагностика ошибок компиляции и лестница подсказок.
     - `Groups_and_Schedule.md` — Учебные группы учеников, календарь и расписание уроков.
     - `Admin_and_QA.md` — Админ-панель, логи аудита (`AuditLog`) и QA-манипуляторы базы данных.
     - `EGE_Generator.md` — Генератор вариантов КЕГЭ и ОГЭ, конструктор шаблонов вариантов.
     - `Parents_Cabinet.md` — Кабинет родителя, привязка аккаунтов (`FamilyTies`) и доступ.
     - `File_Storage.md` — Интеграция с S3 (Boto3) и локальное файловое хранилище.
     - `Onboarding_and_Reminders.md` — Онбординг новых учеников и Celery-планировщик напоминаний.
     - `Tasks_Rubricator.md` — База задач ЕГЭ и рубрикатор тем (карта знаний).
   - Обновлен файл `Core_Functions.md` — в него добавлены ссылки на все 16 дочерних модулей в виде Wikilinks для красивого отображения в Graph View.

---

### [14.07.2026 19:50]

#### Выполненные задачи:
1. **Перевод всей базы знаний Obsidian на русский язык:**
   - Переименованы все файлы и папки базы знаний в Obsidian Vault на русский язык:
     - `Features/` -> `Функционал/`
     - `Testing/` -> `Тестирование/`
     - `Tasks/` -> `Задачи/`
     - Все файлы внутри переименованы в транслит/русский (например, `Tech_Stack.md` -> `Стек_технологий.md`, `Changelog.md` -> `История_изменений.md`).
   - Написан и успешно выполнен автоматический Python-скрипт для переноса файлов и обновления всех перекрестных Wikilinks `[[ ]]` и абсолютных Markdown-ссылок `file:///` на новые русские названия.
2. **Дальнейшее расширение базы знаний (4 новые спецификации):**
   - Созданы 4 новые подробные статьи в папке `Функционал/`:
     - `Деплой_и_девопс.md` — Описание регламента Blue-Green релизов, скрипта деплоя, проверки готовности `/ready` и ролей PostgreSQL/Redis.
     - `Логирование_и_мониторинг.md` — Структура JSON-логов, ядро `logging_core.py`, метрики рантайма `runtime_state.py` и админский роут метрик.
     - `Безопасность_и_защита.md` — Защита от CSRF, токенизация JWT для Trainer API, Flask-Limiter и RBAC-пермишены.
     - `Константы_и_окружение.md` — Справочник констант (`constants.py`), настройки лимитов MMR и песочницы, переменные `.env`.
   - Обновлен файл `Ядро_функционала.md` (добавлено 4 новых ссылки, расширив список дочерних модулей до 20).

---

### [14.07.2026 20:00]

#### Выполненные задачи:
1. **Повышение безопасности панели диагностики (`diagnostics.py`):**
   - Роуты `/admin/diagnostics` и `/admin/diagnostics/api` (которые содержали критические данные окружения, структуры БД и количества записей) защищены декоратором `@require_admin`. Ранее к ним имел доступ любой зарегистрированный пользователь (например, `student` или `parent`).
2. **Исправление и доработка режима обслуживания (Maintenance Mode):**
   - Переработана функция `check_maintenance_mode` в `app/utils/hooks.py`. Ранее проверка режима обслуживания работала только при `ENVIRONMENT = sandbox`. Теперь проверка работает во всех окружениях (включая `production` и `local`).
   - Добавлено важное исключение для администраторов и создателей: если текущий пользователь авторизован и имеет роль `admin` или `creator`, он НЕ перенаправляется на страницу техработ. Это позволяет администрации беспрепятственно работать с платформой во время техработ и при необходимости отключить режим обслуживания через админку.

---

### [14.07.2026 20:05]

#### Выполненные задачи:
1. **Переработка мобильного скелета навигации и отступов:**
   - Десктопный верхний навбар `_dock_nav.html` скрыт на экранах мобильных устройств (добавлен класс `hidden md:flex`), чтобы он не перекрывал контент.
   - Мобильный TabBar (`_mobile_menu.html`) полностью переписан с чистого листа. Добавлена новая группировка табов в зависимости от роли (`student`, `parent`, `tutor`/`admin`/`creator`).
   - Реализована премиальная iOS-style шторка Bottom Sheet с размытием заднего фона, скруглениями углов, ручкой перетаскивания и свитчером темы оформления прямо в шторке.
   - Базовый шаблон `base.html` обновлен: инлайн-стили отступов главного контейнера `<main>` заменены на адаптивные классы `pt-6 pb-24 md:pt-32 md:pb-12`.
2. **Написание CSS-переопределений с чистого листа:**
   - Переписан файл `static/mobile-overrides.css`: заданы стили нижней панели, тактильные микро-анимации табов, анимации выезжания шторки Bottom Sheet и автоматическая адаптация Bento карточек и полей ввода на ширину 100%.
3. **Исправление ошибок в Jinja2-шаблонах:**
   - Исправлена критическая синтаксическая ошибка в `templates/assignments_list.html` (`]{%; }` вместо `}`), которая возвращала ошибку HTTP 500 при открытии списка работ тьюторов.
4. **Тестирование системы:**
   - Flask-сервер успешно протестирован локально, HTTP-смоук тесты под всеми ролями успешно пройдены (`Release HTTP smoke passed.`).

---

### [14.07.2026 20:10]

#### Выполненные задачи:
1. **Расширение базы знаний по модулю Библиотеки:**
   - Дополнен и обновлен файл спецификации `Теория_и_библиотека.md` в Obsidian Vault.
   - Добавлено подробное описание преподавательской Библиотеки материалов (загрузка шпаргалок, опорных конспектов, файлов) и шаблонов уроков (LessonRoomTemplates).
   - Описаны схемы СУБД для таблиц `MaterialAssets`, `LessonMaterialLinks`, `LessonRoomTemplates` и все соответствующие Flask-роуты (`/library/materials`, `/library/lesson-templates`).

---

### [15.07.2026 11:40]

#### Выполненные задачи:
1. **Консолидация правил агента в AGENTS.md:**
   - Все правила из `.cursorrules` (роль, качество кода, impact analysis, алгоритм работы) перенесены в `AGENTS.md`.
   - Обновлены устаревшие пути Obsidian (английские имена → русские: `История_изменений.md`, `Задачи/Список_задач.md`, `Функционал/`, `Тестирование/`).
   - Добавлен протокол старта сессии (читать CHANGELOG + Todo перед работой).
   - Git-правило скорректировано: коммит только по явной просьбе пользователя (не автопуш).
   - `.cursorrules` заменён на ссылку-редирект на `AGENTS.md` — единый источник правил.

---

### [15.07.2026 11:42]

#### Выполненные задачи:
1. **Усиление `.cursorrules`:**
   - Добавлено жёсткое правило: агент всегда обязан читать и следовать `AGENTS.md` перед работой, в начале сессии и перед крупными задачами.
   - При конфликте правил приоритет у `AGENTS.md`.
   - Добавлен чеклист старта сессии (AGENTS.md → CHANGELOG → Todo).

---

### [15.07.2026 12:05]

#### Выполненные задачи:
1. **Глубокая доработка мобильной версии (зона 3):**
   - Добавлен компактный **mobile topbar** (логотип, уведомления, профиль) — `_dock_nav.html`.
   - Переписан `mobile-overrides.css`: паттерн `.table-responsive`, фикс конфликта с admin-таблицами, стек inline grid-ов, KPI-сетки, admin-фильтры, граф связей, QA-виджет.
   - Обернуты таблицы в `.table-responsive`: `admin_users`, `admin_testers`, `admin_topics`, `tester_entities`.
   - **Task Workspace**: мобильный stack-порядок панелей (условие → IDE → вывод), скрыт TabBar на странице IDE, улучшен topbar.
   - Исправлены KPI-сетки в `assignments_list` и `assignment_view`.
   - Billing subscriptions: убран `min-width: 280px` у поиска.
   - `assignment_create`: горизонтальный скролл wizard steps.
   - Обновлены cache-bust версии CSS в `base.html` и `task_workspace.html`.

---

### [15.07.2026 12:20]

#### Выполненные задачи:
1. **Перефокус спринта (библиотека + задачи + генератор):**
   - C++ вынесен из скоупа — убраны упоминания из планов тестирования, Todo и чек-листов.
   - Telegram test-боты отложены до учебного сезона (лето, учеников нет).
2. **Библиотека:**
   - Добавлен переключатель разделов `library/_library_nav.html` (Материалы / Шаблоны / Теория).
   - Исправлен баг заголовков `text-3xl md:text-lg` → `text-2xl md:text-3xl`.
3. **Задачи и генератор:**
   - `auto_grade_answer` теперь использует `normalize_answer_value` — корректное сравнение `3,5` и `3.5`.
   - Placeholder стартового кода в генераторе: только Python.

---

### [Текущая дата]

#### Интеграция Инструментария QA (Собственный баг-трекер):
1. **База данных и Админка (QA Control Room):**
   - Созданы таблицы `QATestCase`, `QAReport`, `QAReportHistory` для хранения тест-кейсов и багов внутри платформы (отказ от JSON и Google Drive).
   - Выполнена миграция старых тестов из `tests_db.json` в базу данных.
   - Разработана панель администратора `/admin/qa` для мониторинга найденных багов, управления статусами (В работе, Решен, Отклонен) и просмотра скриншотов.

2. **Рабочее место тестировщика (`/qa`):**
   - Разработан чистый веб-интерфейс (без отвлекающих элементов), доступный тестировщикам с мобильных устройств и ПК.
   - Добавлена страница прохождения тест-кейса с чек-листом шагов и выбором вердикта (Всё успешно, Минорный баг, Критичный баг).
   - Внедрена возможность отправки "Спонтанных багов" (Ad-hoc) с указанием зоны тестирования.

3. **Скриншотер по Ctrl+V:**
   - Реализована функция загрузки изображений в комментарии к багу напрямую из буфера обмена.
   - Скриншоты асинхронно сохраняются в папку `static/uploads/qa/` и автоматически встраиваются в текст репорта как кликабельные HTML-изображения, видимые в дашборде админа.

4. **Полный отказ от сторонних программ:**
   - Десктопное приложение `tester_assistant_app` на базе Electron полностью выведено из эксплуатации. Процесс QA переведен в закрытый веб-контур платформы BooStudy.

### [Текущая дата]

#### Интеграция Инструментария QA (Этап 1 - Рабочее место тестировщика):
1. **Интерфейс тестировщика (`/qa`):**
   - Разработан и внедрен новый раздел для QA инженеров. Доступ предоставляется пользователям с ролями `tester`, `chief_tester`, а также администраторам.
   - Главная страница раздела отображает список всех активных тест-кейсов, сгруппированных по зонам тестирования (например, "Кодерская", "Библиотека").
   - Добавлена возможность отправлять "Спонтанный баг" (ad-hoc) для ошибок, найденных вне рамок заготовленных тест-кейсов.

2. **Прохождение тест-кейсов (`/qa/test/<id>`):**
   - Реализована карточка прохождения теста с четким отображением шагов воспроизведения (в виде чек-листа) и ожидаемого результата.
   - Интегрирована система вынесения вердикта: `success` (Всё успешно), `minor` (Минорный баг), `critical` (Критичный баг).
   - При выборе бага появляется обязательное поле для комментария.

3. **Скриншотер в браузере (Магия Ctrl+V):**
   - Реализована функция загрузки скриншотов напрямую из буфера обмена. Тестировщик может сделать скриншот средствами ОС и нажать `Ctrl+V` (или "Вставить") прямо в поле комментария.
   - Картинка асинхронно загружается на эндпоинт `/api/qa/upload`, сохраняется в `/uploads/qa/` и автоматически прикрепляется к тексту в формате `[Скриншот прикреплен: url]`.
   - Появление бейджа предпросмотра при успешной загрузке.

4. **Полный отказ от сторонних приложений:**
   - Инструмент тестировщика полностью перенесен в web-версию. Старое Desktop-приложение (Electron) и скрипты `gdrive_service.py` больше не используются для сбора багов.
1. **Файловый менеджер ученика (Workspace):**
   - Установлен суммарный лимит в 50 МБ на все загруженные файлы в рамках одного задания.
   - Теперь система проверяет сумму размеров всех файлов ученика в текущей директории перед загрузкой нового. Это предотвращает переполнение диска.

2. **Интерактивный холст (Canvas):**
   - Исправлен баг, из-за которого холст не находил нужный контейнер и рисовал «в воздухе» в Task Workspace. Добавлена привязка координат к селектору `.tw-task-panel .tw-task-content` с использованием `dataset.assignmentTaskId`.
   - В шаблоне `task_workspace.html` добавлена кнопка вызова холста.

3. **Стресс-тестирование Песочницы (Sandbox):**
   - Написан скрипт многопоточного нагрузочного тестирования `/scripts/qa_load_test_sandbox.py` (имитация 20 конкурентных запросов).
   - Тест пройден успешно: бесконечные циклы `while True:` убиваются ровно через 2.0с по TimeOut, ресурсоемкие списки возвращают `Runner Error`, а валидный код выполняется мгновенно без блокировок.

4. **Режим обслуживания (Maintenance Mode):**
   - Исправлено поведение для AJAX/Fetch запросов (API). Если клиент запрашивает API-эндпоинт во время техработ, бэкенд больше не возвращает HTML-редирект (что ломало парсеры на фронтенде), а отдает четкий JSON `{"error": "maintenance"} c HTTP 503`.

#### Планируемые задачи (В очереди):
- [ ] Ожидание результатов ручного тестирования от QA-инженеров по всем 7 зонам.


---

### [$(date +'%d.%m.%Y %H:%M')]

#### Выполненные задачи:
1. **Подготовка структуры для обновления биллинга:**
   - Создан файл базы знаний `Функционал/Тарифные_планы.md` в Obsidian Vault. Файл будет выступать единым источником правды для хранения всех актуальных цен и условий тарифов перед их переносом в HTML.

---

### [$(date +'%d.%m.%Y %H:%M')]

#### Выполненные задачи:
1. **Обновление публичной страницы биллинга:**
   - В шаблоне `billing_plans_public.html` удален режим "Тех работы" для цели "ОГЭ" и форматов подготовки "Solo" и "God Mode".
   - Теперь карточки полностью кликабельны и участвуют в мастере выбора тарифа.

---

### [$(date +'%d.%m.%Y %H:%M')]

#### Выполненные задачи:
1. **Наполнение базы знаний тарифами:**
   - Файл `Функционал/Тарифные_планы.md` в Obsidian перезаписан в чистовом, структурированном виде (markdown).
   - Извлечены текущие цены из HTML (ЕГЭ и ОГЭ по Информатике, форматы Solo, Mentor, God Mode).
   - Созданы пустые шаблоны-заглушки для "ОГЭ — Математика", "ЕГЭ — Математика (Профиль)" и "ЕГЭ — Математика (База)" для дальнейшего заполнения пользователем.

---

### [$(date +'%d.%m.%Y %H:%M')]

#### Выполненные задачи:
1. **Динамический рендеринг цен на странице биллинга:**
   - Шаблон `billing_plans_public.html` переведен на динамическую подстановку цен через JS (`PRICING_DB`).
   - Добавлены 4 раздела на первом шаге: ОГЭ, ЕГЭ База, ЕГЭ Информатика, ЕГЭ Профиль.
   - Унифицированы пакеты (Sprint, Season Pass, Career Mode, Lead) для Solo и аналогичные для Mentor/God Mode.
   - На карточки `Career Mode` (5 мес) и `Lead` (10 мес) добавлены градиентные бейджи "HOT" и подпись "цена ниже*".
   - На страницу с итоговой ценой добавлено примечание "*акция действует до 31 июля 2026 года 23:59".

---

### [$(date +'%d.%m.%Y %H:%M')]

#### Выполненные задачи:
1. **Редизайн страницы биллинга:**
   - Интерфейс страницы тарифов (`billing_plans_public.html`) полностью переписан в стилистике платформы (мягкие скругления, тени, поддержка темной и светлой темы через `data-theme`).
   - На первом шаге введена явная группировка по классам: "ЕГЭ (11 класс)" с карточками Информатика, Математика (Профиль), Математика (База) и "ОГЭ (9 класс)" с карточками Информатика, Математика.
   - JavaScript логика переведена на генерацию карточек тарифов (шаг 3) напрямую из базы `PRICING_DB`, чтобы не дублировать HTML-блоки.

---

### [$(date +'%d.%m.%Y %H:%M')]

#### Выполненные задачи:
1. **Режим ознакомления на странице тарифов:**
   - На финальном этапе выбора тарифа (`step-6`) полностью убраны формы симуляции оплаты картой и авторизации.
   - Вместо них добавлена плашка "Технические работы" (желтый алерт-бокс).
   - Оставлен только текст об ознакомительном режиме и кнопка связи через Telegram для ручного оформления тарифов.
