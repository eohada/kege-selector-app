# Быстрая диагностика проблем Railway (Production vs Sandbox)

## 🚀 Быстрый старт

1. Откройте диагностический endpoint в обоих окружениях:
   - **Production**: `https://your-app.railway.app/admin/diagnostics`
   - **Sandbox**: `https://your-sandbox-app.railway.app/admin/diagnostics`

2. Сравните данные и найдите различия

3. Следуйте инструкциям ниже для решения проблем

## 🔍 Что проверять

### 1. База данных
- ✅ **Подключение**: Должно быть "OK" в обоих окружениях
- ✅ **Таблицы**: Должно быть одинаковое количество таблиц
- ✅ **Данные**: Проверьте количество записей (может быть пустая БД в sandbox)

### 2. Переменные окружения
- ✅ **ENVIRONMENT**: Должно быть `production` или `sandbox`
- ✅ **DATABASE_URL**: Должен быть установлен в обоих
- ✅ **SECRET_KEY**: Должен быть установлен и уникален для каждого окружения

### 3. Приложение
- ✅ **SECRET_KEY**: Должен быть установлен
- ✅ **CSRF защита**: Должна быть включена

## ❌ Типичные проблемы

### Проблема: Пустая база данных в sandbox

**Симптомы:**
- В sandbox 0 учеников, 0 уроков
- Функционал не работает

**Решение:**
1. Синхронизируйте данные из production:
   ```bash
   python scripts/sync_to_sandbox.py
   ```
2. Или вручную скопируйте данные через Railway CLI

### Проблема: Ошибка подключения к БД

**Симптомы:**
- В диагностике "Database connection: ERROR"
- Ошибки в логах Railway

**Решение:**
1. Проверьте, что PostgreSQL сервис запущен в Railway
2. Проверьте переменную `DATABASE_URL` в настройках окружения
3. Убедитесь, что используется правильный URL (внутренний или внешний)

### Проблема: Разные переменные окружения

**Симптомы:**
- Разные значения `ENVIRONMENT`, `SECRET_KEY` и т.д.

**Решение:**
1. В Railway откройте каждое окружение
2. Settings → Variables
3. Убедитесь, что все необходимые переменные установлены
4. Убедитесь, что `SECRET_KEY` уникален для каждого окружения

## 📋 Контрольный список

Перед обращением за помощью проверьте:

- [ ] Диагностический endpoint открывается в обоих окружениях
- [ ] DATABASE_URL установлен в обоих окружениях
- [ ] Подключение к БД работает (статус "OK")
- [ ] Таблицы созданы (проверьте количество)
- [ ] Данные синхронизированы (если нужно)
- [ ] SECRET_KEY установлен и уникален
- [ ] ENVIRONMENT установлен правильно
- [ ] Логи не показывают критичных ошибок

## 📞 Получение помощи

Если проблема не решена, соберите:

1. **Скриншоты диагностики** из обоих окружений
2. **Логи запуска** из Railway (Deployments → последний деплой)
3. **Список переменных окружения** (Settings → Variables)

И создайте issue с этой информацией.

