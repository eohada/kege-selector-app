# Руководство по тестированию RBAC системы

## 🔑 Учетные данные для входа

После выполнения `python scripts/seed_rbac_data.py` созданы следующие тестовые пользователи:

### 👨‍💼 Администратор
- **Логин:** `admin`
- **Пароль:** `admin123`
- **Email:** admin@example.com
- **Роль:** admin
- **Доступ:** Полный доступ ко всем функциям системы

### 👩‍🏫 Тьюторы
- **Логин:** `tutor1`
- **Пароль:** `tutor123`
- **Email:** tutor1@example.com
- **Роль:** tutor
- **Доступ:** Видит только своих учеников (student1, student2)

- **Логин:** `tutor2`
- **Пароль:** `tutor123`
- **Email:** tutor2@example.com
- **Роль:** tutor
- **Доступ:** Видит только своего ученика (student3)

### 👨‍🎓 Ученики
- **Логин:** `student1`
- **Пароль:** `student123`
- **Email:** student1@example.com
- **Роль:** student
- **Тьютор:** tutor1
- **Родитель:** parent1

- **Логин:** `student2`
- **Пароль:** `student123`
- **Email:** student2@example.com
- **Роль:** student
- **Тьютор:** tutor1
- **Родитель:** parent1

- **Логин:** `student3`
- **Пароль:** `student123`
- **Email:** student3@example.com
- **Роль:** student
- **Тьютор:** tutor2
- **Родитель:** parent2

### 👨‍👩‍👧 Родители
- **Логин:** `parent1`
- **Пароль:** `parent123`
- **Email:** parent1@example.com
- **Роль:** parent
- **Дети:** student1, student2

- **Логин:** `parent2`
- **Пароль:** `parent123`
- **Email:** parent2@example.com
- **Роль:** parent
- **Дети:** student3

## 🚀 Как войти в систему

1. Откройте страницу входа: `/login`
2. Введите **логин** (username) и **пароль** из таблицы выше
3. Нажмите "Войти"

## 🧪 Сценарии тестирования

### 1. Тестирование роли Администратор

**Вход:** `admin` / `admin123`

**Что проверить:**
- ✅ Видит всех пользователей в системе
- ✅ Может создавать/редактировать/удалять пользователей
- ✅ Видит все данные всех учеников
- ✅ Доступ ко всем функциям администрирования
- ✅ Может управлять ролями пользователей

**Маршруты для проверки:**
- `/dashboard` - должен видеть всех учеников
- `/admin/users` - управление пользователями (если реализовано)
- `/student/<id>/analytics` - статистика любого ученика

### 2. Тестирование роли Тьютор

**Вход:** `tutor1` / `tutor123`

**Что проверить:**
- ✅ Видит только своих учеников (student1, student2)
- ✅ НЕ видит student3 (он у tutor2)
- ✅ Может создавать уроки для своих учеников
- ✅ Может проверять домашние задания
- ✅ Видит контакты родителей своих учеников (с маскированием)
- ✅ НЕ может видеть полные контакты (телефон/email замаскированы)

**Маршруты для проверки:**
- `/dashboard` - должен видеть только student1 и student2
- `/student/<student1_id>/analytics` - должен иметь доступ
- `/student/<student3_id>/analytics` - должен получить 403 Forbidden
- `/student/<student1_id>` - профиль ученика, должен видеть секцию "Представители" с замаскированными контактами

**Вход:** `tutor2` / `tutor123`

**Что проверить:**
- ✅ Видит только student3
- ✅ НЕ видит student1 и student2

### 3. Тестирование роли Ученик

**Вход:** `student1` / `student123`

**Что проверить:**
- ✅ Видит только свою статистику
- ✅ Может просматривать свои уроки
- ✅ НЕ может видеть данные других учеников
- ✅ НЕ может создавать уроки
- ✅ НЕ может управлять пользователями

**Маршруты для проверки:**
- `/dashboard` - должен видеть только себя или редирект на свой профиль
- `/student/<student1_id>/analytics` - должен иметь доступ
- `/student/<student2_id>/analytics` - должен получить 403 Forbidden

### 4. Тестирование роли Родитель

**Вход:** `parent1` / `parent123`

**Что проверить:**
- ✅ Видит дашборд родителя: `/parents/parent/dashboard`
- ✅ Может переключаться между детьми (student1, student2)
- ✅ Видит статистику своих детей
- ✅ Видит предстоящие уроки
- ✅ Видит финансовую информацию (если access_level = 'full' или 'financial_only')
- ✅ НЕ может видеть данные чужих детей
- ✅ НЕ может редактировать уроки

**Маршруты для проверки:**
- `/parents/parent/dashboard` - главный дашборд родителя
- `/parents/parent/dashboard?student_id=<student1_id>` - статистика student1
- `/parents/parent/dashboard?student_id=<student3_id>` - должен получить 403 Forbidden (не его ребенок)

**Вход:** `parent2` / `parent123`

**Что проверить:**
- ✅ Видит только student3
- ✅ НЕ видит student1 и student2

## 🔒 Тестирование Data Scoping (Ограничение видимости данных)

### Проверка фильтрации данных по ролям

1. **Войдите как tutor1**
2. Откройте `/dashboard`
3. Проверьте, что видны только student1 и student2
4. Попробуйте открыть профиль student3 напрямую через URL
5. Должна быть ошибка 403 Forbidden

### Проверка маскирования контактов

1. **Войдите как tutor1**
2. Откройте профиль student1: `/student/<student1_id>`
3. Найдите секцию "Представители"
4. Проверьте, что:
   - Телефон замаскирован: `+7 900 *** ** 67`
   - Email замаскирован: `p***@example.com`
   - Telegram замаскирован аналогично

## 📋 Чек-лист тестирования

- [ ] Администратор видит всех пользователей
- [ ] Тьютор видит только своих учеников
- [ ] Тьютор НЕ может видеть чужих учеников (403)
- [ ] Тьютор видит замаскированные контакты родителей
- [ ] Ученик видит только свою статистику
- [ ] Ученик НЕ может видеть чужие данные (403)
- [ ] Родитель видит дашборд с селектором детей
- [ ] Родитель видит статистику своих детей
- [ ] Родитель НЕ может видеть чужих детей (403)
- [ ] Все роли могут войти в систему
- [ ] Пароли работают корректно
- [ ] Сессии сохраняются после входа

## 🐛 Отладка проблем

### Проблема: Не могу войти

1. Проверьте, что пользователь создан:
   ```python
   python scripts/check_profiles.py
   ```

2. Проверьте, что пароль правильный (в скрипте seed_rbac_data.py)

3. Проверьте, что `is_active = True`:
   ```python
   from app import create_app, db
   from core.db_models import User
   app = create_app()
   with app.app_context():
       user = User.query.filter_by(username='admin').first()
       print(f"Active: {user.is_active}, Role: {user.role}")
   ```

### Проблема: Не вижу данные

1. Проверьте связи Enrollment (ученик-тьютор):
   ```python
   from core.db_models import Enrollment
   enrollments = Enrollment.query.all()
   for e in enrollments:
       print(f"Student {e.student_id} -> Tutor {e.tutor_id}")
   ```

2. Проверьте связи FamilyTie (родитель-ученик):
   ```python
   from core.db_models import FamilyTie
   ties = FamilyTie.query.all()
   for t in ties:
       print(f"Parent {t.parent_id} -> Student {t.student_id}")
   ```

## 📝 Примечания

- Все пароли простые для тестирования, в production их нужно изменить
- Email адреса тестовые (@example.com)
- Телефоны и Telegram ID также тестовые
- Для production нужно создать реальные учетные данные
ф
