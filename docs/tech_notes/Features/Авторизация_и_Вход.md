---
status: stable
domain: Авторизация и Профиль
type: action
---
# Авторизация и Вход

**Статус интеграции:** #status/stable (Зелёный - полностью интегрировано и протестировано)

## 🔗 Зависимости (Что должно быть готово ДО интеграции этой фичи)
- (Отсутствуют)

## 💻 Текущий бэкенд
- **Роуты:** `/auth/login`, `/auth/register`, `/auth/logout` (Файл: `app/auth/routes.py`)
- **Таблицы БД:** `users`, `user_profiles`, `user_roles`, `user_consents`
- **Связанные макеты:** `templates/auth/login.html`, `templates/auth/register.html`

## 📝 План интеграции
- [x] Замена устаревшей верстки `templates/auth/login.html` на новый Tactile 3D Bento макет с сохранением WTForms `LoginForm` (`username`, `password`, `form.hidden_tag()`).
- [x] Замена `templates/auth/register.html` на Tactile 3D Bento макет с передачей `username`, `email`, `password`, `role` и поддержкой параметров приглашений (`tutor_id`, `invite_parent_to`, `ref`).
- [x] Проверка рендеринга скрытого CSRF-токена (`csrf_token()`) и безопасная передача POST-запросов.
- [x] Оформление 3D-алертов для flash-сообщений (`get_flashed_messages`) и ошибок валидации форм (неверный пароль, существующий пользователь).
- [x] Сохранение логики редиректов `_redirect_after_login` для учеников, преподавателей, родителей и администраторов.


