# План: эталонная база заданий и LLM (тренажёр)

Подробный пошаговый план: структура эталона, правила сложности, фазы от текущей базы до эталонной разметки, обучения LLM (помощник + создатель заданий) и детектора поведения. Каждый пункт разложен по полочкам.

---

## 1. Текущее состояние (что есть сейчас)

### 1.1. Типы заданий ЕГЭ

- Файл: `data/analytics_kege_difficulty.json`.
- Для каждого номера 1–27 заданы:
  - `task_number`, `topic`, `node_code`, `base_rating`, `exam_points`, `complexity_tier`.
- `complexity_tier`: Trivial, Basic, Intermediate, Advanced, Hard, Expert, Boss — **на уровне типа**, не отдельной задачи.
- Используется: сид узлов знаний, API аналитики, прогноз балла.

### 1.2. База заданий (Tasks)

- Таблица `Tasks`: `task_id`, `task_number`, `content_html`, `answer`, `knowledge_node_id` и др.
- Привязка к узлу: по `knowledge_node_id` → узел имеет один `base_rating` на весь тип.
- **Нет:** поля «сложность этой конкретной задачи» (лёгкое/среднее/тяжёлое).
- Итог: все задачи одного типа в аналитике считаются одинаковой сложности.

### 1.3. Тренажёр и знания

- Каталог `trainer_knowledge/tasks/`: JSON по отдельным задачам (`task_id`, `task_number`, `reference_solution`, `hint_ladder`, `common_mistakes`, `tests`).
- LLM в тренажёре не обучена на этих данных; заготовки можно использовать как основу эталонов.
- Нет единого формата «эталонный прототип по типу × уровень сложности».

### 1.4. Аналитика и детектор

- `app/analytics/engine.py`: обновление рейтинга по узлу после проверки ответа, приём `time_spent_sec`.
- При правильном ответе и времени < 10 сек: `k_factor` умножается на 0.1 (рейтинг почти не растёт).
- **Нет:** проверки «задача тяжёлая»; нет категорий «Невнимательность» / «Списывание»; нет проверки «время < 5 сек + неверный ответ».

---

## 2. Целевое состояние (к чему идём)

### 2.1. Эталон

- По каждому типу (1–27) — набор эталонных прототипов с явной сложностью: лёгкое / среднее / тяжёлое.
- В прототипе: формулировка, эталонное решение, типовые ошибки, лестница подсказок.
- Хранение: единый формат (JSON), один каталог или расширение текущего `trainer_knowledge`.

### 2.2. База заданий

- У каждой задачи в БД (или в справочнике) — уровень сложности: лёгкое / среднее / тяжёлое.
- Разметка согласована с правилами и эталонами; при добавлении новых заданий — правило или LLM + проверка человеком.

### 2.3. LLM

- Обучена на эталонах.
- **Помощник:** подсказки в тренажёре по лестнице, без полного решения.
- **Создатель/классификатор:** по новой формулировке предлагает тип задания, уровень сложности, черновик разбора; сохранение только после проверки человеком.

### 2.4. Детектор

- Быстрый провал: < 5 сек, неверный ответ → «Невнимательность».
- Быстрый успех на тяжёлой задаче: < 10 сек, верный ответ → не завышать рейтинг, при необходимости метка «Списывание / Знает наизусть».
- Используется разметка сложности конкретной задачи (тяжёлое), а не только тип.

---

## 3. Структура эталона (подробно)

### 3.1. Уровни сложности задачи

Три уровня для **конкретной задачи** (инстанса):

| Уровень   | Код (пример) | Назначение | Когда использовать в подборе |
|-----------|----------------|------------|------------------------------|
| Лёгкое    | `easy`         | Разогрев, тренировка, автопроверка | Начало темы, закрепление |
| Среднее   | `medium`       | Основная практика | Обычные уроки и ДЗ |
| Тяжёлое   | `hard`         | Контроль, экзамен, сложный срез | Проверочные, экзамен |

Связь с текущим `complexity_tier` типа (Trivial/Basic/…/Boss): лёгкое задание обычно из типа с низким tier или нижней части диапазона; тяжёлое — из высокого tier или верхней части. Правила задаём явно в конфиге/таблице по `task_number`.

### 3.2. Поля эталонного прототипа (по полочкам)

Один эталонный прототип = один JSON-объект (или запись в БД). Рекомендуемые поля:

| Поле | Обязательность | Описание | Пример / формат |
|------|----------------|----------|------------------|
| `task_number` | да | Номер задания ЕГЭ (1–27) | `24` |
| `node_code` | да | Код узла знаний из analytics_kege_difficulty | `"STRINGS-HARD"` |
| `topic` | да | Название темы (для людей и LLM) | `"Обработка символьных строк"` |
| `difficulty` | да | Уровень: `easy` / `medium` / `hard` | `"hard"` |
| `statement_html` или `statement_text` | да | Формулировка задачи (как в КЕГЭ) | HTML или plain text |
| `reference_solution` | да | Пошаговое решение + итоговый ответ | Текст или структура (шаги + ответ) |
| `correct_answer` | по необходимости | Итоговый ответ для автопроверки | строка/число |
| `common_mistakes` | да | 1–3 типовые ошибки | массив строк |
| `hint_ladder` | желательно | Подсказки по уровням 1–3 | массив `{ "level": 1, "hint": "…" }` |
| `source` | опционально | Откуда задача (открытый банк, год) | для трассировки |
| `id` / `slug` | опционально | Уникальный идентификатор прототипа | для ссылок и версий |

Файловая структура (пример):

- `data/reference_prototypes/` или `trainer_knowledge/reference/`
  - по типам: `by_type/01_GRAPH-BASICS.json` (массив прототипов по типу),  
  - или по одному файлу на прототип: `by_type/01/easy_001.json`, `01/medium_001.json`, `01/hard_001.json`.

### 3.3. Хранение сложности в БД (решение: вариант A)

**Решение:** однозначно выбираем **вариант A** — поле непосредственно в таблице `Tasks`.

**Почему:** сложность — такое же фундаментальное свойство задачи, как текст условия. Хранение в `Tasks` избавляет от лишней нагрузки: при подборе заданий на урок не нужен JOIN с отдельной таблицей.

**Реализация:**

- Добавить в модель `Task` (таблица `Tasks`) поле **`difficulty_level`**.
- Тип поля: **Enum** (easy / medium / hard) или **Int** (см. маппинг ниже). При использовании Int удобно хранить значение 1–10 для совместимости с градацией «лёгкое / среднее / тяжёлое» и формулами рейтинга.
- Миграция: добавить колонку (nullable на период заполнения), затем заполнить в Фазе 2.

### 3.4. Маппинг Easy / Medium / Hard для БД и AnalyticsEngine (Elo)

Движок аналитики (Elo) оперирует числовым рейтингом задачи. Нужен однозначный перевод уровня сложности в значение для формул.

**Таблица маппинга (утвердить в Фазе 0):**

| Метка (для методистов) | Значение в БД (`Task.difficulty_level`) | Рейтинг задачи для Elo (в движке) |
|------------------------|----------------------------------------|-----------------------------------|
| **Easy** (лёгкое)      | 1–3 (или код 1)                        | `Base_Node_Rating - 100`          |
| **Medium** (среднее)   | 4–7 (или код 2)                        | `Base_Node_Rating`                |
| **Hard** (тяжёлое)     | 8–10 (или код 3)                       | `Base_Node_Rating + 150`          |

- **Base_Node_Rating** — базовый рейтинг узла знаний (`KnowledgeNode.base_rating`), к которому привязана задача.
- Итог: методисты оперируют понятными метками («эта задача сложная»), движок получает числа для формул. В `AnalyticsEngine` при расчёте вероятности и обновлении рейтинга использовать рейтинг задачи из этой таблицы (а не только `base_rating` узла), если у задачи заполнено `difficulty_level`.

### 3.5. Хранение лестницы подсказок (hint_ladder) в БД

В плане описан JSON-формат подсказок в эталонных прототипах. Чтобы хранить подсказки у конкретной задачи в БД (для тренажёра и LLM):

- В таблице **`Tasks`** (или в связанной таблице **`TaskAssets`**, если подсказки вынесены в отдельную сущность) добавить поле типа **JSONB** для хранения лестницы подсказок.
- **Имя поля:** например `hints` или `hint_ladder`.
- **Структура JSON:** массив объектов с уровнями и текстом подсказки:
  ```json
  [{"level": 1, "text": "Направление / идея..."}, {"level": 2, "text": "Структура решения..."}, {"level": 3, "text": "Наводка без полного решения..."}]
  ```
- **Пример в модели (SQLAlchemy, PostgreSQL):**
  ```python
  # В модели Task (Tasks)
  hints = db.Column(postgresql.JSONB)  # или db.JSON с поддержкой JSONB в PostgreSQL
  ```
- При использовании SQLite или иной БД без JSONB — хранить как `db.JSON` или `db.Text` с валидным JSON; при миграции на PostgreSQL при необходимости сменить тип на JSONB.

---

## 4. Правила разметки сложности (подробно)

### 4.1. Кто задаёт правила

- Владелец продукта / методист: по каждому типу задания (1–27) решает, по каким признакам задача считается лёгкой, средней или тяжёлой.
- Разработчик: переносит правила в конфиг или код (таблица, JSON, скрипт).

### 4.2. Примеры признаков для правил

- **Статистика решений (приоритетный сигнал):** если по задаче есть исторические данные (кто решал, верно/неверно), доля успешных решений — лучший критерий сложности, чем только текст условия или мнение LLM. Примеры: ~90% решили → Easy; ~20% → Hard; промежуточные значения → Medium. Пороги утвердить и использовать как основной или дополнительный сигнал при разметке (см. Фазу 2).
- **Объём условия:** короткое / среднее / длинное (пороги по числу символов или абзацев).
- **Число шагов в решении:** 1–2 шага → easy, 3–5 → medium, 6+ или неочевидная цепочка → hard.
- **Типовые ошибки:** если в задаче заложен «капкан» из списка типовых ошибок эталона — сдвиг в сторону medium/hard.
- **Экспертная метка:** эталонные прототипы уже размечены (easy/medium/hard); по ним калибруют автоматические правила.
- **Базовый рейтинг узла:** для типа с высоким `base_rating` чаще тяжёлые; с низким — чаще лёгкие (при необходимости — веса по диапазонам).

### 4.3. Результат правил

- Таблица или конфиг: по `task_number` — описание правил (или пороги), как вычислить/предсказать `easy`/`medium`/`hard` для новой или существующей задачи.
- Опционально: скрипт, который по формуле/порогам выставляет черновую сложность; человек выборочно проверяет и правит.

---

## 5. Пошаговый план по фазам (разложено по полочкам)

### Фаза 0. Подготовка (без смены продакшена) ✅ ВЫПОЛНЕНО

Цель: ничего не ломать, зафиксировать решения и форматы.

#### 0.1. Формат эталонного прототипа ✅

- [x] JSON Schema: `data/reference_prototypes/prototype_schema.json`
- [x] Шаблон-пример: `data/reference_prototypes/_template_example.json` (задание №1)
- [x] Решение: один файл на прототип (`task_XX/<difficulty>/proto_NNN.json`)
- [x] Каталог: `data/reference_prototypes/` + README.md с описанием структуры
- [x] Описание всех полей зафиксировано в schema и README

#### 0.2. Правила отнесения к easy / medium / hard ✅

- [x] Конфиг: `data/difficulty_rules.json` — по всем 27 типам с критериями easy/medium/hard
- [x] Указаны base_elo для каждого типа и max_points
- [x] Определены пороги по статистике: `correct_rate >= 0.80 → Easy, 0.40–0.80 → Medium, < 0.40 → Hard`
- [x] Приоритет классификации зафиксирован: 1) Статистика решений, 2) Шаги решения, 3) Подвохи, 4) Экспертиза

#### 0.3. Хранение сложности в БД (вариант A) ✅

- [x] В модели `Tasks` добавлено поле `difficulty_level` (Integer 1–10, nullable=True, index=True)
- [x] Миграция в `app/utils/db_migrations.py` — ALTER TABLE Tasks ADD COLUMN difficulty_level INTEGER
- [x] Индекс `ix_tasks_difficulty_level`
- [x] Property `difficulty_label` → 'easy'/'medium'/'hard' по значению difficulty_level
- [x] Константы: `DIFFICULTY_EASY_MAX = 3`, `DIFFICULTY_MEDIUM_MAX = 7`, `DIFFICULTY_HARD_MIN = 8`

#### 0.4. Маппинг сложности для AnalyticsEngine ✅

- [x] Метод `Tasks.get_elo_rating()`: Easy → base_rating − 100, Medium → base_rating, Hard → base_rating + 150
- [x] `app/analytics/engine.py` использует `task.get_elo_rating()` вместо `node.base_rating`
- [x] При `difficulty_level = NULL` → Medium (без смещения)
- [x] Детектор поведения встроен в `process_submission()`:
  - `fast_fail`: ответ за < 5 сек + неверно → k_factor × 0.5
  - `fast_success_hard`: ответ за < 10 сек + верно + Hard → k_factor × 0.1
- [x] Логирование поведенческих аномалий

#### 0.5. Хранение лестницы подсказок (hint_ladder) в БД ✅

- [x] В модели `Tasks` добавлено поле `hints` (JSON/JSONB, nullable=True)
- [x] Структура: `[{"level": 1, "text": "…"}, {"level": 2, "text": "…"}, …]`
- [x] Миграция: ALTER TABLE Tasks ADD COLUMN hints JSONB (PostgreSQL) / JSON (SQLite)

#### 0.6. Расширение AnalyticsEvent ✅

- [x] Добавлены поля: `task_id`, `time_spent_sec`, `behavior_flags` (JSON)
- [x] Миграция для всех трёх полей
- [x] `process_submission()` записывает task_id, time_spent_sec и behavior_flags

#### 0.7. Скрипты и инструменты ✅

- [x] `scripts/auto_label_difficulty.py` — автоматическая разметка difficulty_level по статистике решений
  - Источники: LessonTask, Answer, AnalyticsEvent
  - Поддержка `--dry-run`, `--min-answers`, `--force`
- [x] `scripts/export_training_data.py` — экспорт обучающих данных для LLM
  - Формирование JSONL для fine-tuning (решения, подсказки, сложность)
  - CSV сводка и отчёт о покрытии

#### 0.8. Чек-лист готовности к Фазе 1 ✅

- [x] Формат эталона утверждён (JSON Schema) и записан
- [x] Каталог для эталонов создан (`data/reference_prototypes/`)
- [x] Правила по типам записаны (`data/difficulty_rules.json`)
- [x] Решение по БД зафиксировано: `Tasks.difficulty_level`, маппинг для Elo, `Tasks.hints` (JSONB)
- [x] Детектор поведения встроен в AnalyticsEngine
- [x] Скрипт авторазметки готов
- [x] Скрипт экспорта training data готов

---

### Фаза 1. Эталонный набор (в работе)

Цель: иметь минимум по одному эталонному прототипу на тип и уровень (где применимо).

**Текущий статус:** покрытие по типам 1–27 и по уровням easy/medium/hard есть (81 файл, валидация пройдена). Синхронизация эталонов 19–21 в банк заданий настроена в удалённой админке. Остаётся ревизия контента: полнота подсказок, типовых ошибок и эталонного решения. Скрипт: `scripts/check_prototype_content_readiness.py`.

#### 1.1. Приоритет типов

- [ ] Определить порядок: например сначала типы 1–10, потом 11–20, потом 21–27; или по приоритету для тренажёра.
- [ ] Для каждого типа решить: нужны ли все три уровня (easy, medium, hard) или достаточно 1–2 (напр. только medium и hard).

#### 1.2. Сбор содержимого по одному прототипу

Для каждого запланированного «ячейки» (тип × уровень):

- [ ] Взять или составить формулировку задачи (открытый банк, своя, из текущей базы).
- [ ] Написать эталонное решение (пошагово + ответ).
- [ ] Выписать 1–3 типовые ошибки.
- [ ] Написать лестницу подсказок (уровень 1 — идея, 2 — структура, 3 — наводка без решения).
- [ ] Заполнить все обязательные поля по формату из п. 3.2.

#### 1.3. Сохранение в едином формате

- [ ] Сохранить каждый прототип в выбранном каталоге в выбранной структуре (файл на прототип или файл на тип).
- [ ] Проверить, что JSON валидный и что все обязательные поля присутствуют (при желании — скрипт валидации).

#### 1.4. Ревизия и покрытие

- [ ] Проверить: по каждому типу 1–27 есть хотя бы один прототип (хотя бы одного уровня).
- [ ] Отметить типы, где пока один уровень — позже добавить остальные.
- [ ] Чек-лист: «Эталонный набор минимально готов для обучения LLM».

---

### Фаза 2. Выравнивание базы заданий (разметка сложности)

Цель: у каждой задачи в текущей базе (и при добавлении новых) есть уровень сложности.

**Важно:** не полагаться только на текст условия и на классификацию LLM. Если есть исторические данные по решениям (кто решал задачу, с каким результатом), их нужно использовать как **основной или сильный дополнительный сигнал** для сложности. Статистика решений — более надёжный критерий, чем только мнение LLM по формулировке.

#### 2.1. Подготовка скрипта/инструмента

- [ ] Реализовать загрузку правил из п. 0.2 (конфиг/таблица).
- [ ] **Приоритет: исторические данные.** Реализовать расчёт доли успешных решений по задаче (по проверенным ответам / сдачам). Примеры порогов: задача решается в ~90% случаев → склонять к **Easy**; в ~20% случаев → к **Hard**; промежуточные значения → **Medium**. Пороги утвердить (например, 70% / 40% или свои).
- [ ] Реализовать получение сложности по задаче: сначала по статистике решений (если данных достаточно), иначе по правилам (объём, шаги и т.д.) или по умолчанию `medium`.
- [ ] Опционально: вызов LLM «классификатор типа + сложность» по тексту задачи — использовать как **дополнительный сигнал** или черновик для задач без статистики; не сохранять автоматически, только после согласования с человеком или с учётом статистики.

#### 2.2. Разметка существующих задач

- [ ] Выгрузить список задач с `task_id`, `task_number`, `knowledge_node_id`, текст условия (и при необходимости ответ).
- [ ] Для задач с историей решений: вычислить долю верных решений; по порогам присвоить черновой уровень (easy/medium/hard).
- [ ] Для задач без достаточной статистики: вычислить/предсказать уровень по правилам (п. 4.2) и при необходимости по LLM; трактовать результат как черновик.
- [ ] Сохранить результат во временный файл или таблицу: `task_id` → `difficulty_level`.
- [ ] Выборочная проверка человеком (например, по 5–10% или по всем тяжёлым): правки перенести в итоговую разметку.

#### 2.3. Запись в выбранное хранилище

- [ ] Если поле в `Tasks`: обновить записи (UPDATE) из итоговой разметки; применить миграцию, если ещё не применяли.
- [ ] Если отдельная таблица: заполнить её из итоговой разметки.
- [ ] Если пока файл/справочник: записать итоговый JSON/CSV и подключить его к коду аналитики и подбора заданий.

#### 2.4. Новые задачи

- [ ] Определить процесс: при добавлении новой задачи сложность выставляется по правилам (или по умолчанию `medium`), при необходимости — пересчёт батчем раз в N дней.
- [ ] Если есть UI добавления заданий: опционально показывать предсказание сложности (от LLM или правил) с возможностью правки человеком.

#### 2.5. Чек-лист Фазы 2

- [ ] Все текущие задачи с привязкой к узлу имеют уровень сложности.
- [ ] Хранилище (БД или файл) обновлено и используется в коде.
- [ ] Документирован процесс для новых заданий.

---

### Фаза 3. Обучение LLM

Цель: модель умеет давать подсказки и при необходимости классифицировать тип/сложность.

#### 3.1. Подготовка датасета

- [ ] Собрать из эталонных прототипов (Фаза 1) единый датасет: формулировка, эталонное решение, типовые ошибки, подсказки, тип задания, уровень сложности.
- [ ] Разделить на train/val (и при необходимости test).
- [ ] Добавить вариации формулировок или аугментации, чтобы модель не копировала только эталонные фразы.
- [ ] Явно пометить в инструкциях: «не выдавать полное решение в подсказках».

#### 3.2. Задачи модели

- [ ] **Задача 1 — помощник:** по формулировке задачи и (опционально) текущему шагу ученика выдавать подсказку уровня 1, 2 или 3; не выдавать полное решение.
- [ ] **Задача 2 (опционально) — классификатор:** по тексту задачи предсказывать `task_number` (или node_code) и `difficulty` (easy/medium/hard).
- [ ] Выбрать: одна модель на обе задачи или две отдельные (например, одна для подсказок, одна для классификации).

#### 3.3. Обучение и оценка

- [ ] Обучить/дообучить выбранную модель на собранном датасете.
- [ ] Оценить качество: подсказки релевантны, не раскрывают решение; классификация (если есть) совпадает с экспертной разметкой на отложенной выборке.
- [ ] Зафиксировать версию модели и датасета (артефакты, путь или тег).

#### 3.4. Интеграция

- [ ] Подключить модель к тренажёру: при запросе подсказки отправлять формулировку (и контекст) в модель, возвращать подсказку нужного уровня.
- [ ] Если есть инструмент разметки/создания заданий: подключить классификатор для предзаполнения типа и сложности; сохранение только после проверки человеком.

#### 3.5. Чек-лист Фазы 3

- [ ] Датасет собран и описан.
- [ ] Модель обучена и проверена.
- [ ] Тренажёр использует модель для подсказок.
- [ ] При необходимости классификатор интегрирован в инструмент заданий.

---

### Фаза 4. Тренажёр: помощник и создатель заданий

Цель: ученики получают подсказки без списывания; преподаватель может создавать/размечать задания с помощью модели.

#### 4.1. Помощник (подсказки)

- [ ] В тренажёре при запросе подсказки: определить тип задания (по задаче или по контексту), при необходимости уровень сложности.
- [ ] Запросить у модели подсказку уровня 1, 2 или 3 (по политике: сначала 1, потом 2, потом 3).
- [ ] Показывать подсказку ученику; не показывать эталонное решение и ответ.
- [ ] Логировать запросы подсказок (для анализа и дообучения).

#### 4.2. Создатель заданий (инструмент для преподавателя)

- [ ] Реализовать сценарий: ввод новой формулировки (вставка текста или импорт/ссылка).
- [ ] Вызвать классификатор: предложить `task_number` (тип), `difficulty`, при желании черновик разбора/решения.
- [ ] Показать предложение преподавателю; дать возможность отредактировать тип, сложность и текст.
- [ ] Сохранение в базу только после явного подтверждения (и при необходимости модерации).

#### 4.3. Общая база эталонов

- [ ] И помощник, и создатель используют одни и те же эталонные прототипы (и при необходимости одну и ту же модель).
- [ ] При обновлении эталонов (добавление прототипов, правки подсказок) предусмотреть пересбор датасета и переобучение/дообучение модели по регламенту.

#### 4.4. Чек-лист Фазы 4

- [ ] Подсказки в тренажёре работают и не выдают решение.
- [ ] Инструмент создания/разметки заданий использует модель и сохраняет только после проверки человеком.
- [ ] Процесс обновления эталонов и модели описан.

---

### Фаза 5. Детектор поведения

Цель: учитывать «быстрый провал» и «быстрый успех на тяжёлом» в аналитике и не искажать рейтинг.

#### 5.1. Модуль классификации события

- [ ] Создать модуль: например `core/analytics/behavior.py` или `app/analytics/behavior.py`.
- [ ] Вход: `task_id`, `is_correct`, `time_spent_sec`, при необходимости `user_id`, `submission_id`.
- [ ] Получить сложность задачи: из БД (поле в Tasks или отдельная таблица) или из справочника; если нет разметки — считать задачу «не тяжёлой» для детектора.
- [ ] Правила:
  - если `time_spent_sec < 5` и неверный ответ → категория `carelessness` (Невнимательность);
  - если `time_spent_sec < 10`, правильный ответ и задача **тяжёлая** → категория `fast_success_hard` (Списывание / Знает наизусть).
- [ ] Выход: категория события (или «норма») и флаг «не увеличивать рейтинг» для быстрого успеха на тяжёлом.

#### 5.2. Интеграция с engine

- [ ] В `app/analytics/engine.py` (или в месте вызова `process_submission`) перед обновлением рейтинга вызвать детектор.
- [ ] Если детектор вернул «быстрый успех на тяжёлом»: применить ослабление K (как сейчас при времени < 10 сек, но только для задач с разметкой «тяжёлое»).
- [ ] Если детектор вернул «быстрый провал»: при желании не менять рейтинг или менять слабее; опционально только записать категорию в события.
- [ ] Сохранять категорию в аналитике (например, в `AnalyticsEvent` добавить поле `behavior_category` или писать в отдельную таблицу событий детектора).

#### 5.3. Константы и настройки

- [ ] Пороги вынести в конфиг или константы: 5 сек для провала, 10 сек для успеха, список категорий.
- [ ] Документировать: при отсутствии разметки сложности «тяжёлое» не считается (не штрафуем за быстрый успех).

#### 5.4. Отчёты (опционально)

- [ ] При желании: отчёт по ученику или по группе — сколько событий «Невнимательность», сколько «Быстрый успех на тяжёлом» (для методиста/преподавателя).

#### 5.5. Чек-лист Фазы 5

- [ ] Модуль детектора реализован и покрыт правилами из спецификации.
- [ ] Рейтинг не завышается при быстром успехе на тяжёлой задаче.
- [ ] Быстрый провал классифицируется; при необходимости учтён в рейтинге или только в логах.
- [ ] Сложность для детектора берётся из разметки базы (Фаза 2).

---

## 6. Риски и ограничения

- **Качество эталонов:** от него зависят подсказки и разметка; начинать с малого набора и расширять.
- **Переобучение LLM:** в датасет добавлять вариации; в инструкциях явно запрещать выдавать полное решение в подсказках.
- **Человек в цикле:** разметка и создание заданий всегда с финальным подтверждением человеком.
- **Производительность:** вызов LLM при каждом запросе подсказки — учесть лимиты и кэш при необходимости.

---

## 7. Связь с существующими файлами

| Что | Где |
|-----|-----|
| Типы заданий ЕГЭ, base_rating, tier | `data/analytics_kege_difficulty.json` |
| Узлы знаний | БД `knowledge_nodes`, сид `scripts/seed_analytics_kege.py` |
| Задачи | БД `Tasks`, привязка `knowledge_node_id`; после Фазы 0 — поля `difficulty_level`, `hints` (JSONB) |
| Рейтинг задачи для Elo | По маппингу п. 3.4: Base_Node_Rating ± смещение в зависимости от `difficulty_level` |
| Заготовки знаний для тренажёра | `trainer_knowledge/tasks/*.json` |
| Рейтинг и время решения | `app/analytics/engine.py` (`process_submission`, `time_spent_sec`) |
| Детектор | Пока только ослабление K при ответе < 10 сек в `engine.py` |

---

## 8. Краткий чек-лист по фазам

| Фаза | Главный результат |
|------|-------------------|
| 0 | Формат эталона, правила сложности, поле `difficulty_level` и `hints` (JSONB) в `Tasks`, маппинг Easy/Medium/Hard для Elo зафиксированы |
| 1 | Минимальный эталонный набор по типам 1–27 (хотя бы по одному прототипу на тип/уровень) |
| 2 | Вся база заданий размечена по сложности (с приоритетом статистики решений); новым заданиям выставляется уровень |
| 3 | LLM обучена; интегрирована в тренажёр (подсказки) и при необходимости в создатель заданий |
| 4 | Подсказки работают без выдачи решения; создатель заданий с проверкой человеком |
| 5 | Детектор реализован; быстрый провал и быстрый успех на тяжёлом учтены в аналитике |

Дальнейший шаг: Фаза 1 в работе — ревизия контента (подсказки, типовые ошибки, эталонное решение); затем Фаза 2 (разметка сложности базы).
