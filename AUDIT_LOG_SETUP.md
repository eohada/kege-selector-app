# Инструкция по настройке системы логирования

## ✅ Что реализовано

1. **Модели данных:**
   - `Tester` - идентификация тестировщиков
   - `AuditLog` - логи всех действий

2. **Сервис логирования:**
   - Асинхронная очередь (не блокирует основной поток)
   - Автоматическое извлечение информации о тестировщике
   - Логирование IP, User-Agent, времени выполнения

3. **Middleware:**
   - Автоматическая идентификация тестировщиков
   - Логирование просмотров страниц
   - Логирование AJAX запросов

4. **JavaScript трекинг:**
   - Отслеживание кликов по кнопкам
   - Отслеживание отправки форм
   - Отслеживание AJAX запросов
   - Отслеживание изменений в селектах

5. **Админ-панель:**
   - Просмотр всех логов с фильтрами
   - Статистика по тестировщикам
   - Экспорт в CSV

6. **Ротация логов:**
   - Команда `flask rotate-audit-logs` для архивирования логов старше недели

## 🚀 Настройка

### 1. Добавить audit-tracker.js во все шаблоны

Добавь в конец каждого шаблона (перед `</body>`) строку:

```html
<script src="{{ url_for('static', filename='audit-tracker.js') }}"></script>
```

Шаблоны для обновления:
- `templates/dashboard.html` ✅ (уже добавлен)
- `templates/schedule.html`
- `templates/student_profile.html`
- `templates/kege_generator.html`
- `templates/lesson_homework.html`
- `templates/results.html`
- `templates/student_form.html`
- `templates/lesson_form.html`
- `templates/update_plans.html`
- `templates/lesson_mode.html`
- И другие...

### 2. Установить переменную окружения для админ-панели

#### Для локальной разработки:

Создай файл `.env` (см. `.env.example`) и добавь:

```bash
ADMIN_SECRET=твой-секретный-ключ-здесь
```

#### Для Render (staging):

1. Зайди в Dashboard → Web Service → Environment
2. Добавь переменную `ADMIN_SECRET` (или используй автоматическую генерацию в `render.yaml`)
3. Скопируй значение `ADMIN_SECRET` из Render Dashboard
4. Используй его для доступа к админ-панели:
   ```
   https://твой-сайт.onrender.com/admin-audit?secret=ТВОЙ_ADMIN_SECRET
   ```

**Важно:** 
- Используй сложный случайный ключ для production!
- Сгенерировать можно через: `python -c "import secrets; print(secrets.token_urlsafe(32))"`
- Сохрани ключ в безопасном месте!

### 3. Инициализировать таблицы в БД

При первом запуске таблицы создадутся автоматически через `db.create_all()`.

Если нужно вручную:

```bash
flask shell
>>> from app import db
>>> from core.db_models import Tester, AuditLog
>>> db.create_all()
```

### 4. Настроить ротацию логов (опционально)

Добавь в cron или планировщик задач:

```bash
# Каждый день в 3:00 ночи
0 3 * * * cd /path/to/app && flask rotate-audit-logs
```

Или на Render через Scheduled Jobs.

## 📊 Использование админ-панели

### Доступ к админ-панели

Открой в браузере:

```
https://твой-сайт.onrender.com/admin-audit?secret=твой-секретный-ключ
```

### Фильтры

- **Тестировщик** - фильтр по конкретному тестировщику
- **Действие** - фильтр по типу действия (page_view, create, update, etc.)
- **Сущность** - фильтр по сущности (Student, Lesson, Page, etc.)
- **Статус** - фильтр по статусу (success, error, warning, info)
- **Дата от/до** - фильтр по времени

### Экспорт

Нажми кнопку "📥 Экспорт в CSV" для скачивания отфильтрованных логов.

## 🔍 Анализ логов

### Типы действий

- `page_view` - просмотр страницы
- `page_loaded` - загрузка страницы (JavaScript)
- `click` - клик по кнопке/ссылке
- `click_primary` - клик по основной кнопке
- `click_danger` - клик по опасной кнопке
- `form_submit` - отправка формы
- `select_change` - изменение селекта
- `ajax_request` - AJAX запрос
- `ajax_error` - ошибка AJAX
- `create` - создание сущности
- `update` - обновление сущности
- `delete` - удаление сущности

### Сущности

- `Page` - страница
- `Student` - ученик
- `Lesson` - урок
- `Task` - задание
- `Form` - форма
- `Button` - кнопка
- `API` - API запрос

### Статусы

- `success` - успешное выполнение
- `error` - ошибка
- `warning` - предупреждение
- `info` - информационное событие

## 🛠️ Расширенное использование

### Логирование в коде

```python
from core.audit_logger import audit_logger

# Простое логирование
audit_logger.log(
    action='create_student',
    entity='Student',
    entity_id=student.student_id,
    status='success',
    metadata={'name': student.name}
)

# Логирование ошибки
audit_logger.log_error(
    action='create_student',
    entity='Student',
    error=str(e),
    traceback=traceback.format_exc()
)
```

### Использование декоратора

```python
from core.audit_decorators import log_action

@app.route('/student/create', methods=['POST'])
@log_action(action='create', entity='Student', get_entity_id=lambda r: r.get_json().get('student_id'))
def create_student():
    # ...
```

## ⚠️ Важные замечания

1. **Производительность:** Логирование асинхронное, но при большом количестве событий может замедлить работу. Следи за размером таблицы `AuditLog`.

2. **Ротация:** Не забывай запускать ротацию логов, иначе таблица будет расти бесконечно.

3. **Безопасность:** Не храни чувствительные данные (пароли, токены) в метаданных логов.

4. **Тестирование:** На staging можно логировать всё, на production лучше ограничить только важными событиями.

## ✅ Выполнено

- [x] Добавить audit-tracker.js во все шаблоны ✅
- [x] Настроить ADMIN_SECRET в конфигурации (render.yaml) ✅
- [x] Обновить документацию ✅

## 📝 TODO

- [ ] Настроить ADMIN_SECRET в переменных окружения Render (после деплоя)
- [ ] Настроить автоматическую ротацию логов (cron/scheduled job)
- [ ] Протестировать работу на staging

