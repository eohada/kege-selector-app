# Пошаговая инструкция: Размещение staging-окружения на Render

## 📋 Подготовка (локально)

### Шаг 1: Проверка готовности проекта

Убедись, что у тебя есть:
- ✅ Аккаунт на GitHub (или другой Git-хостинг)
- ✅ Проект закоммичен и запушен в репозиторий
- ✅ Локальная база данных `data/keg_tasks.db` с данными

### Шаг 2: Проверка файлов конфигурации

Убедись, что в проекте есть следующие файлы:
- ✅ `render.yaml` - конфигурация для Render
- ✅ `requirements.txt` - зависимости (с gunicorn и psycopg2-binary)
- ✅ `scripts/init_staging_db.py` - скрипт для инициализации БД
- ✅ Все шаблоны с `audit-tracker.js`

### Шаг 3: Закоммитить все изменения

```bash
# Проверь статус
git status

# Добавь все изменения
git add .

# Закоммить
git commit -m "Prepare staging deployment with audit logging"

# Запушь в репозиторий
git push origin main
```

---

## 🚀 Деплой на Render

### Шаг 4: Регистрация на Render

1. Зайди на [render.com](https://render.com)
2. Нажми **"Get Started for Free"** или **"Sign Up"**
3. Зарегистрируйся через GitHub (рекомендуется) или email
4. Подтверди email (если требуется)

### Шаг 5: Создание Blueprint (автоматический способ)

1. В Dashboard Render нажми **"New +"** (вверху справа)
2. Выбери **"Blueprint"**
3. Нажми **"Create a new Blueprint"**
4. Подключи свой GitHub аккаунт (если еще не подключен):
   - Нажми **"Connect GitHub"**
   - Разреши доступ к репозиторию
   - Выбери репозиторий с проектом
5. Render автоматически обнаружит `render.yaml` и покажет предпросмотр сервисов
6. Проверь, что будут созданы:
   - **Web Service** (`kege-selector-staging`)
   - **PostgreSQL Database** (`kege-db-staging`)
7. Нажми **"Apply"** или **"Create"**

**Важно:** Render автоматически сгенерирует `SECRET_KEY` и `ADMIN_SECRET`!

### Шаг 6: Ожидание деплоя

1. Render начнет создавать сервисы (это займет 5-10 минут)
2. Следи за прогрессом в Dashboard:
   - Сначала создастся PostgreSQL Database
   - Затем начнется сборка Web Service
   - После сборки запустится приложение

3. **Не закрывай вкладку!** Дождись завершения деплоя

---

## 🔧 Настройка после деплоя

### Шаг 7: Получение ADMIN_SECRET

1. В Dashboard Render найди свой **Web Service** (`kege-selector-staging`)
2. Открой его (кликни на название)
3. Перейди на вкладку **"Environment"**
4. Найди переменную `ADMIN_SECRET`
5. **Скопируй значение** и сохрани в безопасном месте (например, в менеджер паролей)

**Важно:** Этот ключ нужен для доступа к админ-панели логов!

### Шаг 8: Получение URL сайта

1. В том же Web Service найди раздел **"Settings"**
2. Скопируй **"Service URL"** (например, `https://kege-selector-staging.onrender.com`)
3. Сохрани этот URL

### Шаг 9: Инициализация базы данных

1. В Dashboard Render открой свой **Web Service**
2. Перейди на вкладку **"Shell"** (в меню слева)
3. В открывшемся терминале выполни:

```bash
# Установи зависимости (если нужно)
pip install psycopg2-binary

# Запусти скрипт инициализации
python scripts/init_staging_db.py
```

4. Дождись завершения (скрипт скопирует данные из локальной SQLite БД в PostgreSQL)
5. Проверь вывод - должно быть сообщение об успешном копировании

**Если скрипт не работает:**
- Проверь, что файл `scripts/init_staging_db.py` существует
- Проверь, что локальная БД `data/keg_tasks.db` есть в репозитории (или скопируй её вручную)

---

## ✅ Проверка работы

### Шаг 10: Проверка сайта

1. Открой URL сайта в браузере (из Шага 8)
2. Проверь, что сайт загружается
3. Проверь основные функции:
   - [ ] Главная страница (dashboard) открывается
   - [ ] Список учеников отображается
   - [ ] Можно создать нового ученика
   - [ ] Расписание работает
   - [ ] Генератор заданий работает

### Шаг 11: Проверка админ-панели

1. Открой админ-панель (замени `ТВОЙ_ADMIN_SECRET` на значение из Шага 7):
   ```
   https://твой-сайт.onrender.com/admin-audit?secret=ТВОЙ_ADMIN_SECRET
   ```
2. Проверь, что:
   - [ ] Админ-панель открывается (не показывает 403)
   - [ ] Статистика отображается
   - [ ] Логи видны (если уже были действия)

### Шаг 12: Тестирование логирования

1. Открой сайт в новом окне браузера (или в режиме инкогнито)
2. При первом заходе должно появиться окно с запросом имени
3. Введи тестовое имя (например, "Тестер1")
4. Поброди по сайту:
   - Открой несколько страниц
   - Кликни по кнопкам
   - Создай или отредактируй ученика
5. Вернись в админ-панель и обнови страницу
6. Проверь, что:
   - [ ] События появились в логах
   - [ ] Твое тестовое имя отображается
   - [ ] Фильтры работают

---

## 👥 Подготовка для тестировщиков

### Шаг 13: Подготовка инструкции для тестировщиков

Создай простую инструкцию для друзей:

```
Привет! 

Я создал тестовую версию сайта для поиска багов.

1. Открой: https://твой-сайт.onrender.com
2. При первом заходе введи своё имя (чтобы я мог видеть, кто что делает)
3. Просто используй сайт как обычно - ломай, ищи баги, тестируй всё!
4. Если найдешь проблему - просто опиши, что произошло

Важно: это тестовая версия, все изменения не сохраняются навсегда.
```

### Шаг 14: Передача доступа

1. Отправь друзьям:
   - URL сайта
   - Краткую инструкцию (из Шага 13)
2. **НЕ передавай** `ADMIN_SECRET` - он только для тебя!

---

## 📊 Мониторинг

### Шаг 15: Регулярная проверка логов

1. Каждый день (или по необходимости) заходи в админ-панель
2. Проверяй:
   - Какие действия выполняют тестировщики
   - Какие ошибки возникают
   - Какие страницы они посещают
3. Используй фильтры для анализа:
   - Фильтр по тестировщику - чтобы видеть действия конкретного человека
   - Фильтр по ошибкам - чтобы видеть все проблемы
   - Фильтр по дате - чтобы видеть активность за период

### Шаг 16: Экспорт логов (опционально)

1. В админ-панели примени нужные фильтры
2. Нажми **"📥 Экспорт в CSV"**
3. Сохрани файл для дальнейшего анализа

---

## 🔄 Обновление staging

### Шаг 17: Обновление данных (если нужно)

Если нужно обновить данные в staging БД:

1. Открой Render Shell (как в Шаге 9)
2. Запусти скрипт заново:
   ```bash
   python scripts/init_staging_db.py
   ```

### Шаг 18: Обновление кода

Если ты внес изменения в код:

1. Закоммить и запушь изменения:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```
2. Render автоматически пересоберет и перезапустит сервис
3. Следи за прогрессом в Dashboard

---

## ⚠️ Важные замечания

### Ограничения free плана:

- **Web Service засыпает** после 15 минут неактивности
  - Первый запрос после пробуждения будет медленным (30-60 сек)
  - Это нормально для staging!

- **PostgreSQL доступен 90 дней** на free плане
  - После этого нужно будет обновить или перейти на платный план

- **Ограничения ресурсов:**
  - 512 MB RAM
  - 0.5 CPU
  - Может быть медленнее, чем production

### Безопасность:

- ✅ Все изменения изолированы от production
- ✅ Тестировщики не могут получить доступ к админ-панели (без секрета)
- ⚠️ Не храни production секреты в staging окружении
- ⚠️ Не передавай `ADMIN_SECRET` тестировщикам

---

## 🆘 Решение проблем

### Проблема: Сайт не загружается

**Решение:**
1. Проверь логи в Render Dashboard → Web Service → Logs
2. Убедись, что деплой завершился успешно
3. Проверь, что все переменные окружения установлены

### Проблема: Ошибка подключения к БД

**Решение:**
1. Проверь, что PostgreSQL Database создана и запущена
2. Проверь, что `DATABASE_URL` правильно установлен
3. Попробуй перезапустить Web Service

### Проблема: Скрипт инициализации не работает

**Решение:**
1. Проверь, что файл `scripts/init_staging_db.py` существует
2. Проверь, что локальная БД `data/keg_tasks.db` доступна
3. Попробуй запустить скрипт вручную через Shell

### Проблема: Админ-панель показывает 403

**Решение:**
1. Проверь, что `ADMIN_SECRET` правильно установлен в переменных окружения
2. Проверь, что используешь правильный секрет в URL
3. Убедись, что секрет не содержит лишних пробелов

---

## ✅ Чеклист готовности

Перед тем как дать доступ тестировщикам, убедись:

- [ ] Сайт загружается и работает
- [ ] База данных инициализирована
- [ ] Админ-панель доступна
- [ ] Логирование работает (видишь события в админ-панели)
- [ ] `ADMIN_SECRET` сохранен в безопасном месте
- [ ] URL сайта готов для передачи тестировщикам
- [ ] Инструкция для тестировщиков подготовлена

---

**Готово!** Теперь у тебя есть полноценная staging-песочница для тестировщиков! 🎉

