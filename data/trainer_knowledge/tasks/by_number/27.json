{
  "task_id": 0,
  "task_number": 27,
  "language": "python",
  "title": "Задание 27: Эффективные алгоритмы",
  "common_mistakes": [
    "Неверный размер буфера (K или K-1).",
    "Ошибки при вычислении комплементарного остатка (`(3 - r) % 3`)."
  ],
  "hint_ladder": [
    {
      "level": 1,
      "hint": "Используй очередь (список) длиной 5 для хранения чисел, которые пока 'слишком близко'."
    },
    {
      "level": 2,
      "hint": "Когда число выпадает из очереди, оно становится доступным для создания пары. Учитывай его в массиве остатков `rems`."
    },
    {
      "level": 3,
      "hint": "Для текущего числа `x` прибавляй к ответу `rems[(3 - x%3) % 3]`."
    }
  ],
  "reference_solution": "**Шаг 1.** Эффективный алгоритм ($O(N)$). Используем буфер и счетчики остатков. \nМы идем по последовательности с индексом `i`. В пару к `x = nums[i]` может попасть любое число `y = nums[j]`, где `j <= i - 5`. \nВсе такие числа `y` уже прошли через 'карантин' (буфер длиной 5). \nМы будем хранить количество чисел, которые уже вышли из карантина, разбитое по остаткам от деления на 3 (rems[0], rems[1], rems[2]).\n\n```python\nwith open('27_B.txt') as f:\n    n = int(f.readline())\n    nums = [int(line) for line in f]\n\nrems = [0] * 3 # Счетчик остатков для доступных чисел (левее i-5)\nqueue = [] # Буфер (очередь) последних 5 чисел\ncount = 0\nK = 5\n\nfor x in nums:\n    # Если очередь заполнена (в ней K элементов), значит самый старый элемент (x_old) уже 'созрел'\n    if len(queue) == K:\n        x_old = queue.pop(0)\n        rems[x_old % 3] += 1\n    \n    # Теперь ищем пару для текущего x из тех, кто уже в rems\n    # Сумма кратна 3:\n    # x%3==0 -> пара 0\n    # x%3==1 -> пара 2\n    # x%3==2 -> пара 1\n    target_rem = (3 - (x % 3)) % 3\n    count += rems[target_rem]\n    \n    # Добавляем текущий x в очередь (он станет доступен через K шагов)\n    queue.append(x)\n\nprint(count)\n```\n\n**Шаг 2.** Для файла А можно написать простой перебор `for i ... for j in range(i+5, n)`.\n\n```python\n# Перебор для А\n# ...\n```\n\n---\n\n**Альтернативные решения:**\n\n*Решение программой (Эффективное)*:\n- Код выше.\n\n*Решение перебором (только Файл А)*:\n- Два цикла.\n\n\n\n**Ответ:** 347 1066550107",
  "tests": []
}