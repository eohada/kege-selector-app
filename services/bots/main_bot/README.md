# Telegram Бот-Трекер Репортов

Бот для автоматического отслеживания и управления репортами от тестировщиков в Telegram-группе.

## Функционал

- 🔍 **Автоматическое отслеживание** сообщений в группе по тегам:
  - `#BUG` - ошибка функционала
  - `#UIFIX` - ошибка интерфейса/верстки
  - `#FEATURE` - предложение по функционалу

- 📨 **Пересылка репортов** в личку администратору

- ✅ **Управление статусами** через inline-кнопки:
  - 🆕 Новый
  - 🔄 В работе
  - ✅ Решено
  - ❌ Отклонено

- 📢 **Автоматические уведомления** в группу при изменении статуса

- 💾 **Хранение данных** в SQLite базе данных

## Установка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Сохраните токен - он понадобится для настройки

### 3. Узнать свой Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Бот покажет ваш ID
3. Сохраните ID - это будет ID администратора

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта или установите переменные окружения:

```bash
# Токен бота от @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Ваш Telegram ID (от @userinfobot)
TELEGRAM_ADMIN_ID=your_telegram_id_here

# (Опционально) ID группы тестировщиков
TELEGRAM_GROUP_ID=your_group_id_here
```

### 5. Добавление бота в группу

1. Добавьте бота в группу тестировщиков
2. Дайте боту права администратора (чтобы он мог отправлять сообщения)
3. Или просто убедитесь, что бот может читать сообщения в группе

## Запуск

### Локальный запуск

```bash
python -m telegram_bot.bot
```

### Запуск в фоновом режиме (Linux/Mac)

```bash
nohup python -m telegram_bot.bot > telegram_bot.log 2>&1 &
```

### Запуск через systemd (Linux)

Создайте файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Bot Tracker
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/kege_selector_app
Environment="TELEGRAM_BOT_TOKEN=your_token"
Environment="TELEGRAM_ADMIN_ID=your_id"
ExecStart=/usr/bin/python3 -m telegram_bot.bot
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

## Использование

### В группе тестировщиков

Просто отправьте сообщение с одним из тегов:

```
#BUG При нажатии на кнопку "Сохранить" возникает ошибка 500
```

```
#UIFIX На мобильных устройствах кнопки выходят за границы экрана
```

```
#FEATURE Предлагаю добавить возможность экспорта в PDF
```

Бот автоматически:
1. Обнаружит тег
2. Сохранит репорт в базу данных
3. Отправит уведомление администратору в личку

### В личке с ботом (для администратора)

Администратор получает сообщение с репортом и может:

- Нажать **"🔄 В работе"** - изменить статус на "В работе"
- Нажать **"✅ Решено"** - отметить репорт как решенный
- Нажать **"❌ Отклонено"** - отклонить репорт
- Нажать **"📋 Детали"** - посмотреть полную информацию о репорте

При изменении статуса бот автоматически отправит уведомление в группу.

### Команды бота

- `/start` - информация о боте
- `/stats` - статистика репортов (только для администратора)

## Структура базы данных

База данных хранится в `data/reports.db` и содержит таблицу `reports` с полями:

- `id` - автоинкрементный ID
- `report_id` - уникальный идентификатор репорта
- `group_message_id` - ID сообщения в группе
- `group_chat_id` - ID чата группы
- `author_id` - ID автора сообщения
- `author_username` - username автора
- `author_first_name` - имя автора
- `tag` - тег репорта (#BUG, #UIFIX, #FEATURE)
- `content` - текст репорта
- `status` - статус (new, in_progress, resolved, rejected)
- `created_at` - дата создания
- `updated_at` - дата последнего обновления
- `admin_message_id` - ID сообщения в личке админа
- `admin_chat_id` - ID чата админа

## Устранение неполадок

### Бот не отвечает

1. Проверьте, что токен бота правильный
2. Убедитесь, что бот добавлен в группу
3. Проверьте логи на наличие ошибок

### Репорты не пересылаются

1. Проверьте, что `TELEGRAM_ADMIN_ID` установлен правильно
2. Убедитесь, что бот может отправлять сообщения администратору
3. Проверьте, что сообщения содержат правильные теги

### Ошибки базы данных

1. Убедитесь, что директория `data/` существует и доступна для записи
2. Проверьте права доступа к файлу базы данных

## Деплой на Railway

Для постоянной работы бота на хостинге см.:
- [QUICK_RAILWAY_DEPLOY.md](QUICK_RAILWAY_DEPLOY.md) - быстрая инструкция
- [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md) - подробная инструкция

**Кратко:**
1. Создайте новый сервис в Railway
2. Подключите репозиторий
3. Установите переменные окружения
4. Установите Start Command: `python -m telegram_bot.bot`

## Разработка

### Структура проекта

```
telegram_bot/
├── __init__.py          # Инициализация модуля
├── bot.py               # Основной файл бота
├── models.py            # Модели данных (база данных)
├── config.example.py    # Пример конфигурации
└── README.md           # Документация
```

### Добавление новых тегов

Отредактируйте список `TRACKED_TAGS` в файле `bot.py`:

```python
TRACKED_TAGS = ['#BUG', '#UIFIX', '#FEATURE', '#NEW_TAG']
```

### Добавление новых статусов

Отредактируйте словарь `STATUSES` в файле `bot.py`:

```python
STATUSES = {
    'new': '🆕 Новый',
    'in_progress': '🔄 В работе',
    'resolved': '✅ Решено',
    'rejected': '❌ Отклонено',
    'new_status': '📌 Новый статус'
}
```

## Лицензия

Этот бот является частью проекта "Генератор заданий КЕГЭ".