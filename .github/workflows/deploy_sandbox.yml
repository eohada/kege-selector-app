name: Deploy Sandbox (AUTO)

on:
  push:
    branches:
      - main 

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH into server and update sandbox
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          # The deploy step failed with: "dial tcp ...:22: i/o timeout".
          # Increase SSH connect timeout to reduce flakiness when the host is slow/unreachable.
          timeout: 2m
          command_timeout: 20m
          script: |
            /opt/boostudy/deploy_sandbox.sh