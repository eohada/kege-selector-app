{
  "task_id": 0,
  "task_number": 22,
  "language": "python",
  "title": "Задание 22: Параллельные вычисления",
  "common_mistakes": [
    "Забыли, что время завершения всей партии — это максимум из времен завершения всех процессов (не сумма!).",
    "В Excel некорректно обработали список предков '3;4' (ВПР не сработает)."
  ],
  "hint_ladder": [
    {
      "level": 1,
      "hint": "Время завершения процесса = Его длительность + Время завершения самого медленного предка."
    },
    {
      "level": 2,
      "hint": "В Python используй словарь для хранения данных и рекурсивную функцию для расчета времени."
    },
    {
      "level": 3,
      "hint": "Ответ задачи — это максимальное время завершения среди всех процессов."
    }
  ],
  "reference_solution": "**Шаг 1.** Считаем данные. Структура файла: `ID; Time; Deps`. Разделитель в Deps — точка с запятой.\n\n```python\ndata = {}\nwith open('22_easy.csv') as f:\n    for line in f:\n        # Удаляем лишние пробелы и разбиваем\n        parts = line.strip().split(';')\n        pid = int(parts[0])\n        time = int(parts[1])\n        # Предки могут быть '0' или '1;2'\n        if parts[2] == '0':\n            deps = []\n        else:\n            # Обработка '1; 2' -> [1, 2]\n            deps = [int(x) for x in parts[2].split(';') if x.strip().isdigit()]\n        \n        data[pid] = {'time': time, 'deps': deps, 'end': 0}\n```\n\n**Шаг 2.** Для вычисления времени завершения процесса нужно знать время завершения всех его предков. \n$End(P) = Time(P) + \\max(End(Dep_1), End(Dep_2), \\dots)$. \nИспользуем рекурсию с мемоизацией (или просто словарь, так как ID идут обычно по порядку, но рекурсия надежнее).\n\n```python\ndef get_end_time(pid):\n    if data[pid]['end'] > 0:\n        return data[pid]['end']\n    \n    # Время старта = макс время завершения предков\n    start_time = 0\n    if data[pid]['deps']:\n        start_time = max(get_end_time(dep) for dep in data[pid]['deps'])\n    \n    data[pid]['end'] = start_time + data[pid]['time']\n    return data[pid]['end']\n\n# Вычисляем для всех процессов\nmax_total = 0\nfor pid in data:\n    max_total = max(max_total, get_end_time(pid))\n\nprint(max_total)\n```\n\n---\n\n**Альтернативные решения:**\n\n*Решение программой (Python)*:\n- Полный код решения.\n\n*Решение в Excel*:\n- Открываем файл. Столбцы: A (ID), B (Время), C (Предки).\n- Если предков несколько (через точку с запятой), Excel не поймет это как числа. В Easy задачах обычно 1 предок или 0. \nПусть в столбце D будет время окончания. \nD2: `=B2 + ВПР(C2; A:D; 4; 0)`. \nЕсли C2=0, то просто `=B2` (используйте ЕСЛИ).\n- Ответ = МАКС(столбец D).\n\n\n\n**Ответ:** 805",
  "tests": []
}