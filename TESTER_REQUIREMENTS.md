# Техническое задание для тестировщиков
## Платформа "Чёрный неон" - Генератор заданий КЕГЭ

**Версия документа:** 1.0  
**Дата:** 2025-12-05  
**Целевая аудитория:** Тестировщики (роль `tester`)

---

## 1. ОБЩЕЕ ОПИСАНИЕ ПЛАТФОРМЫ

### 1.1 Назначение
Веб-платформа для автоматизированного подбора заданий КЕГЭ с отслеживанием истории использования, управления учениками, планирования уроков и формирования домашних заданий.

### 1.2 Технологический стек
- **Backend:** Flask (Python)
- **База данных:** SQLite (локально) / PostgreSQL (production)
- **Frontend:** HTML, CSS, JavaScript (vanilla)
- **Авторизация:** Flask-Login, Flask-WTF (CSRF защита)
- **ORM:** SQLAlchemy

### 1.3 Роли пользователей
- **`tester`** (Тестировщик) - основной пользователь платформы
- **`creator`** (Создатель) - администратор с расширенными правами

---

## 2. СИСТЕМА АВТОРИЗАЦИИ

### 2.1 Вход в систему

**Маршрут:** `/login`  
**Метод:** GET, POST  
**Доступ:** Публичный (неавторизованные пользователи)

#### Функционал:
- Форма входа с полями:
  - **Логин (ключ):** текстовое поле, обязательное
  - **Пароль:** поле типа password, обязательное
  - Кнопка "Войти"
- Валидация:
  - Оба поля обязательны для заполнения
  - Проверка существования пользователя
  - Проверка корректности пароля (bcrypt hash)
- Поведение:
  - При успешном входе: редирект на `/dashboard`
  - При ошибке: отображение flash-сообщения об ошибке
  - После входа создается сессия пользователя

#### Ожидаемое поведение:
- ✅ Корректные данные → успешный вход
- ✅ Неверный логин → сообщение "Неверный логин или пароль"
- ✅ Неверный пароль → сообщение "Неверный логин или пароль"
- ✅ Пустые поля → валидация формы (браузерная)
- ✅ CSRF токен должен присутствовать в форме

### 2.2 Выход из системы

**Маршрут:** `/logout`  
**Метод:** GET, POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Завершение сессии пользователя
- Очистка cookies
- Редирект на страницу входа

#### Ожидаемое поведение:
- ✅ После выхода пользователь не может получить доступ к защищенным страницам
- ✅ Редирект на `/login`

### 2.3 Профиль пользователя

**Маршрут:** `/user/profile`  
**Метод:** GET  
**Доступ:** Только авторизованные пользователи (`@login_required`)

#### Функционал:
- Отображение информации о пользователе:
  - **Логин:** username
  - **Роль:** отображаемое название роли (Тестировщик / Создатель)
- Кнопка выхода из системы (красная, с обводкой)
- Аватар пользователя в левом верхнем углу (круглый, с первой буквой логина)

#### Ожидаемое поведение:
- ✅ Отображается корректный логин текущего пользователя
- ✅ Отображается корректная роль
- ✅ Кнопка выхода работает корректно
- ✅ Аватар кликабелен и ведет на профиль

---

## 3. ГЛАВНАЯ СТРАНИЦА И НАВИГАЦИЯ

### 3.1 Главная страница (Dashboard)

**Маршруты:** `/`, `/dashboard`, `/index`, `/home`  
**Метод:** GET  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Поиск по ученикам (глобальный поиск)
- Список всех учеников с карточками
- Фильтрация и сортировка учеников
- Быстрые действия:
  - Создать нового ученика
  - Перейти к расписанию
  - Перейти к генератору заданий

#### Элементы интерфейса:
- **Навигационная панель:**
  - Аватар пользователя (слева вверху)
  - Логотип "Чёрный неон"
  - Ссылки: Ученики, Расписание, Генератор, Планы
- **Поиск:** поле ввода для поиска по имени ученика
- **Карточки учеников:** отображение основной информации

#### Ожидаемое поведение:
- ✅ Поиск работает в реальном времени
- ✅ Карточки учеников отображаются корректно
- ✅ Клик по карточке ведет на профиль ученика
- ✅ Все ссылки навигации работают

### 3.2 Навигация

#### Доступные разделы:
1. **Ученики** (`/dashboard`) - список всех учеников
2. **Расписание** (`/schedule`) - календарь уроков
3. **Генератор** (`/kege-generator`) - генератор заданий
4. **Планы** (`/update-plans`) - планы и заметки

#### Ожидаемое поведение:
- ✅ Все ссылки доступны авторизованным пользователям
- ✅ Навигация работает на всех страницах
- ✅ Активная страница визуально выделена

---

## 4. УПРАВЛЕНИЕ УЧЕНИКАМИ

### 4.1 Создание ученика

**Маршрут:** `/student/new`  
**Метод:** GET, POST  
**Доступ:** Только авторизованные пользователи

#### Форма создания ученика:

**Основная информация:**
- **Имя ученика** (обязательное, минимум 2 символа)
- **ID на платформе** (опциональное, уникальное)
- **Категория** (выпадающий список):
  - Не выбрано
  - ЕГЭ
  - ОГЭ
  - ЛЕВЕЛАП
  - ПРОГРАММИРОВАНИЕ
- **Класс** (1-11 класс, опционально)

**Цели и сроки:**
- **Целевой балл** (0-100, для категорий ЕГЭ/ОГЭ)
- **Цель (текст)** (для категорий ЛЕВЕЛАП/ПРОГРАММИРОВАНИЕ)
- **Сроки** (текстовое поле)

**Дополнительная информация:**
- **Язык программирования** (опционально)
- **Уровень знаний (диагностика)** (опционально)
- **Предпочтения в решении** (опционально)
- **Сильные стороны** (опционально)
- **Слабые стороны** (опционально)
- **Общая оценка** (опционально)
- **Краткое описание** (опционально)
- **Дополнительные заметки** (опционально)

#### Валидация:
- Имя обязательно, минимум 2 символа
- ID на платформе должен быть уникальным (если указан)
- Целевой балл: 0-100 (только для ЕГЭ/ОГЭ)
- Поля зависят от выбранной категории

#### Ожидаемое поведение:
- ✅ Успешное создание → редирект на профиль ученика
- ✅ Ошибки валидации → отображение ошибок под полями
- ✅ Уникальность ID проверяется
- ✅ Поля динамически показываются/скрываются в зависимости от категории

### 4.2 Редактирование ученика

**Маршрут:** `/student/<student_id>/edit`  
**Метод:** GET, POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Открытие формы с предзаполненными данными
- Возможность изменения всех полей
- Сохранение изменений

#### Ожидаемое поведение:
- ✅ Форма предзаполнена текущими данными
- ✅ Валидация работает как при создании
- ✅ Успешное сохранение → редирект на профиль ученика

### 4.3 Профиль ученика

**Маршрут:** `/student/<student_id>`  
**Метод:** GET  
**Доступ:** Только авторизованные пользователи

#### Отображаемая информация:

**Основная информация:**
- Имя ученика
- ID на платформе (если указан)
- Категория
- Класс
- Целевой балл / Цель (текст)
- Сроки

**Статистика:**
- Общее количество уроков
- Количество проведенных уроков
- Количество запланированных уроков
- Прогресс по целям

**Список уроков:**
- Хронологический список всех уроков
- Для каждого урока:
  - Дата и время
  - Тип урока (Обычный / Проверочный / Вводный)
  - Статус (Запланирован / Идет сейчас / Проведен / Отменен)
  - Тема урока
  - Домашнее задание (если задано)
  - Статус ДЗ (Задано, выполнено / Задано, не выполнено / Не задано)

**Действия с уроками:**
- 🎓 Начать урок (для запланированных)
- 📝 Сформировать ДЗ
- 🏫 Сформировать классную работу
- 📊 Сформировать проверочную
- ➖ ДЗ не задано
- ✏️ Редактировать урок
- 🗑️ Удалить урок

#### Ожидаемое поведение:
- ✅ Вся информация отображается корректно
- ✅ Уроки отсортированы по дате (новые сверху)
- ✅ Все действия работают корректно
- ✅ Статистика рассчитывается правильно

### 4.4 Удаление ученика

**Маршрут:** `/student/<student_id>/delete`  
**Метод:** POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Удаление ученика и всех связанных данных (каскадное удаление)
- Подтверждение удаления (через модальное окно)

#### Ожидаемое поведение:
- ✅ Удаление требует подтверждения
- ✅ Удаляются все уроки ученика
- ✅ После удаления → редирект на dashboard
- ✅ Flash-сообщение об успешном удалении

### 4.5 Статистика ученика

**Маршрут:** `/student/<student_id>/statistics`  
**Метод:** GET  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Детальная статистика по ученику:
  - Прогресс по заданиям
  - История выполнения ДЗ
  - Графики прогресса
  - Аналитика по типам заданий

#### Ожидаемое поведение:
- ✅ Статистика рассчитывается корректно
- ✅ Графики отображаются (если реализованы)

---

## 5. УПРАВЛЕНИЕ УРОКАМИ

### 5.1 Создание урока

**Маршрут:** `/student/<student_id>/lesson/new`  
**Метод:** GET, POST  
**Доступ:** Только авторизованные пользователи

#### Форма создания урока:

**Основные поля:**
- **Тип урока** (обязательное):
  - 📚 Обычный урок
  - ✅ Проверочный урок
  - 👋 Вводный урок
- **Часовой пояс** (обязательное):
  - 🕐 Московское время (МСК)
  - 🕐 Томское время (ТОМСК)
- **Дата и время урока** (обязательное, формат: YYYY-MM-DDTHH:MM)
- **Длительность** (обязательное, 15-240 минут, по умолчанию 60)
- **Статус** (обязательное):
  - Запланирован
  - Идет сейчас
  - Проведен
  - Отменен
- **Тема урока** (опционально)
- **Заметки о уроке** (опционально, текстовое поле)
- **Домашнее задание** (опционально, текстовое поле)
- **Статус ДЗ** (обязательное):
  - Задано, выполнено
  - Задано, не выполнено
  - Не задано

#### Валидация:
- Все обязательные поля должны быть заполнены
- Длительность: 15-240 минут
- Дата и время должны быть в будущем (для новых уроков)

#### Ожидаемое поведение:
- ✅ Успешное создание → редирект на профиль ученика
- ✅ Ошибки валидации отображаются
- ✅ Часовой пояс применяется корректно

### 5.2 Редактирование урока

**Маршрут:** `/lesson/<lesson_id>/edit`  
**Метод:** GET, POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Открытие формы с предзаполненными данными
- Возможность изменения всех полей
- Сохранение изменений

#### Ожидаемое поведение:
- ✅ Форма предзаполнена
- ✅ Валидация работает
- ✅ Успешное сохранение → редирект на профиль ученика

### 5.3 Начало урока

**Маршрут:** `/lesson/<lesson_id>/start`  
**Метод:** POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Изменение статуса урока с "Запланирован" на "Идет сейчас"
- Автоматическое обновление времени начала

#### Ожидаемое поведение:
- ✅ Статус изменяется корректно
- ✅ Редирект на профиль ученика

### 5.4 Завершение урока

**Маршрут:** `/lesson/<lesson_id>/complete`  
**Метод:** POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Изменение статуса урока на "Проведен"
- Автоматическое обновление времени завершения

#### Ожидаемое поведение:
- ✅ Статус изменяется корректно
- ✅ Время завершения сохраняется

### 5.5 Удаление урока

**Маршрут:** `/lesson/<lesson_id>/delete`  
**Метод:** POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Удаление урока и всех связанных заданий
- Подтверждение удаления

#### Ожидаемое поведение:
- ✅ Требуется подтверждение
- ✅ Удаляются все связанные задания
- ✅ Редирект на профиль ученика

### 5.6 Управление домашним заданием

#### 5.6.1 Отметка "ДЗ не задано"

**Маршрут:** `/lesson/<lesson_id>/homework/not-assigned`  
**Метод:** POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Установка статуса ДЗ в "Не задано"
- Очистка связанных заданий (если нужно)

#### Ожидаемое поведение:
- ✅ Статус ДЗ изменяется
- ✅ Редирект на профиль ученика

#### 5.6.2 Просмотр домашнего задания

**Маршрут:** `/lesson/<lesson_id>/homework`  
**Метод:** GET  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Отображение всех заданий, привязанных к уроку
- Разделение по типам:
  - Домашнее задание (homework)
  - Классная работа (classwork)
  - Проверочная работа (exam)
- Возможность просмотра заданий
- Возможность оценки выполнения ДЗ

#### Ожидаемое поведение:
- ✅ Все задания отображаются корректно
- ✅ Разделение по типам работает
- ✅ Можно просмотреть каждое задание

---

## 6. ГЕНЕРАТОР ЗАДАНИЙ

### 6.1 Генератор заданий (общий)

**Маршрут:** `/kege-generator`  
**Метод:** GET, POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Выбор типа задания (1-27)
- Указание количества заданий
- Опция "Включить пропущенные задания"
- Генерация набора уникальных заданий
- Поиск заданий по тексту

#### Форма генератора:
- **Тип задания:** выпадающий список (Задание 1, Задание 2, ..., Задание 27)
- **Количество заданий:** числовое поле (1-100)
- **Включить пропущенные:** чекбокс
- Кнопка "Сгенерировать Набор"

#### Ожидаемое поведение:
- ✅ Генерируются только уникальные задания (не использованные ранее)
- ✅ Пропущенные задания включаются только при установке чекбокса
- ✅ После генерации → редирект на `/results`

### 6.2 Генератор заданий для урока

**Маршрут:** `/kege-generator/<lesson_id>`  
**Метод:** GET, POST  
**Доступ:** Только авторизованные пользователи

#### Параметры:
- `assignment_type` (query parameter):
  - `homework` - домашнее задание
  - `classwork` - классная работа
  - `exam` - проверочная работа

#### Функционал:
- То же, что и общий генератор
- Дополнительно: привязка заданий к уроку
- Автоматическое обновление статуса ДЗ урока

#### Ожидаемое поведение:
- ✅ Задания привязываются к уроку
- ✅ Статус ДЗ урока обновляется
- ✅ Тип назначения (homework/classwork/exam) сохраняется

### 6.3 Результаты генерации

**Маршрут:** `/results`  
**Метод:** GET  
**Доступ:** Только авторизованные пользователи

#### Отображаемая информация:
- Список сгенерированных заданий
- Для каждого задания:
  - Номер задания
  - Полный контент задания (HTML)
  - Прикрепленные файлы (если есть)
  - Кнопки действий:
    - ✅ Принять (добавить в урок / отметить как использованное)
    - ⏭️ Пропустить
    - 🚫 В черный список

#### Массовые операции:
- Чекбокс "Выбрать все"
- Кнопки массовых действий:
  - Принять все выбранные
  - Пропустить все выбранные
  - Добавить все выбранные в черный список

#### Ожидаемое поведение:
- ✅ Все задания отображаются корректно
- ✅ Контент заданий рендерится (HTML, формулы, изображения)
- ✅ Файлы можно скачать
- ✅ Действия применяются корректно
- ✅ Массовые операции работают
- ✅ При привязке к уроку задания сохраняются в LessonTask

### 6.4 Действия с заданиями

**Маршрут:** `/action`  
**Метод:** POST  
**Доступ:** Только авторизованные пользователи

#### Параметры (JSON):
- `action`: `accept` | `skip` | `blacklist`
- `task_ids`: массив ID заданий
- `lesson_id`: ID урока (опционально, для привязки)
- `assignment_type`: `homework` | `classwork` | `exam` (опционально)

#### Функционал:
- **Принять (accept):**
  - Если указан `lesson_id`: создается запись в `LessonTask`
  - Если не указан: создается запись в `UsageHistory`
  - Задание больше не будет показываться в генераторе
- **Пропустить (skip):**
  - Создается запись в `SkippedTasks`
  - Задание можно вернуть в выборку позже
- **В черный список (blacklist):**
  - Создается запись в `BlacklistTasks`
  - Задание никогда не будет показываться (необратимо)

#### Ожидаемое поведение:
- ✅ Все действия выполняются корректно
- ✅ При привязке к уроку обновляется статус ДЗ
- ✅ Ответ в формате JSON: `{"success": true/false, "error": "..."}`

### 6.5 Просмотр принятых заданий

**Маршрут:** `/accepted`  
**Метод:** GET  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Список всех принятых заданий
- Фильтрация по типу задания
- Возможность сброса истории использования

#### Ожидаемое поведение:
- ✅ Все принятые задания отображаются
- ✅ Фильтрация работает
- ✅ Сброс истории работает (удаляет записи из UsageHistory)

### 6.6 Просмотр пропущенных заданий

**Маршрут:** `/skipped`  
**Метод:** GET  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Список всех пропущенных заданий
- Возможность вернуть задание в выборку
- Возможность принять задание
- Возможность добавить в черный список
- Массовые операции

#### Ожидаемое поведение:
- ✅ Все пропущенные задания отображаются
- ✅ Возврат в выборку работает (удаление из SkippedTasks)
- ✅ Все действия работают корректно

---

## 7. РАСПИСАНИЕ

### 7.1 Календарь уроков

**Маршрут:** `/schedule`  
**Метод:** GET  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Календарное отображение всех уроков
- Фильтрация по ученикам
- Фильтрация по датам
- Быстрое создание урока
- Просмотр деталей урока

#### Элементы интерфейса:
- Календарь (месячный/недельный вид)
- Список уроков на выбранную дату
- Кнопка "Создать урок"

#### Ожидаемое поведение:
- ✅ Все уроки отображаются в календаре
- ✅ Фильтрация работает
- ✅ Клик по уроку открывает детали
- ✅ Создание урока работает

### 7.2 Создание урока из расписания

**Маршрут:** `/schedule/create-lesson`  
**Метод:** POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Быстрое создание урока с предзаполненной датой
- Выбор ученика из списка
- Заполнение основных полей

#### Ожидаемое поведение:
- ✅ Дата предзаполняется из календаря
- ✅ Успешное создание → обновление календаря

---

## 8. ПЛАНЫ И ЗАМЕТКИ

### 8.1 Страница планов

**Маршрут:** `/update-plans`  
**Метод:** GET, POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Просмотр и редактирование планов
- Заметки по ученикам
- Общие заметки

#### Ожидаемое поведение:
- ✅ Планы сохраняются и отображаются
- ✅ Редактирование работает

---

## 9. ИМПОРТ И ЭКСПОРТ ДАННЫХ

### 9.1 Импорт данных

**Маршрут:** `/import-data`  
**Метод:** GET, POST  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Импорт учеников из файла
- Импорт уроков из файла
- Поддерживаемые форматы: CSV, Excel

#### Ожидаемое поведение:
- ✅ Файлы загружаются корректно
- ✅ Данные валидируются
- ✅ Ошибки импорта отображаются

### 9.2 Экспорт данных

**Маршрут:** `/export-data`  
**Метод:** GET  
**Доступ:** Только авторизованные пользователи

#### Функционал:
- Экспорт расписания
- Экспорт статистики
- Экспорт заданий
- Форматы: CSV, Excel, PDF, Markdown

#### Ожидаемое поведение:
- ✅ Экспорт работает корректно
- ✅ Файлы скачиваются
- ✅ Формат данных корректен

---

## 10. БАЗА ДАННЫХ И МОДЕЛИ

### 10.1 Основные таблицы

#### Tasks (Задания)
- `task_id` (PK)
- `task_number` (1-27)
- `content` (HTML контент)
- `url` (ссылка на задание)
- `files` (JSON массив файлов)

#### Students (Ученики)
- `student_id` (PK)
- `name` (имя)
- `platform_id` (уникальный ID)
- `category` (ЕГЭ/ОГЭ/ЛЕВЕЛАП/ПРОГРАММИРОВАНИЕ)
- `target_score` (целевой балл)
- `goal_text` (текстовая цель)
- И другие поля...

#### Lessons (Уроки)
- `lesson_id` (PK)
- `student_id` (FK → Students)
- `lesson_type` (regular/exam/introductory)
- `lesson_date` (дата и время)
- `duration` (длительность в минутах)
- `status` (planned/in_progress/completed/cancelled)
- `homework_status` (assigned_done/assigned_not_done/not_assigned)
- И другие поля...

#### LessonTasks (Связь уроков и заданий)
- `lesson_task_id` (PK)
- `lesson_id` (FK → Lessons)
- `task_id` (FK → Tasks)
- `assignment_type` (homework/classwork/exam)
- `date_assigned` (дата назначения)

#### UsageHistory (История использования)
- `id` (PK)
- `task_id` (FK → Tasks)
- `student_id` (FK → Students, опционально)
- `used_at` (дата использования)

#### SkippedTasks (Пропущенные задания)
- `id` (PK)
- `task_id` (FK → Tasks)
- `student_id` (FK → Students, опционально)
- `skipped_at` (дата пропуска)

#### BlacklistTasks (Черный список)
- `id` (PK)
- `task_id` (FK → Tasks)
- `student_id` (FK → Students, опционально)
- `blacklisted_at` (дата добавления)

#### Users (Пользователи)
- `id` (PK)
- `username` (уникальный)
- `password_hash` (хеш пароля)
- `role` (tester/creator)
- `is_active` (активен/неактивен)
- `created_at` (дата создания)
- `last_login` (последний вход)

#### AuditLog (Логи аудита)
- `id` (PK)
- `user_id` (FK → Users, опционально)
- `tester_id` (FK → Testers, опционально, для старых логов)
- `action` (тип действия)
- `entity_type` (тип сущности)
- `entity_id` (ID сущности)
- `details` (JSON детали)
- `timestamp` (время действия)

### 10.2 Связи между таблицами

- Student → Lessons (один ко многим)
- Lesson → LessonTasks (один ко многим)
- Task → LessonTasks (один ко многим)
- Task → UsageHistory (один ко многим)
- Task → SkippedTasks (один ко многим)
- Task → BlacklistTasks (один ко многим)
- User → AuditLog (один ко многим)

---

## 11. БЕЗОПАСНОСТЬ И ВАЛИДАЦИЯ

### 11.1 CSRF защита
- Все формы защищены CSRF токенами
- AJAX запросы должны включать CSRF токен
- Токен передается через `csrf_token()` в шаблонах

### 11.2 Авторизация
- Все защищенные маршруты требуют `@login_required`
- Админ-панель доступна только для роли `creator`
- Проверка прав через `current_user.is_creator()`

### 11.3 Валидация данных
- Все формы валидируются на сервере
- Клиентская валидация (HTML5 атрибуты)
- Проверка уникальности (platform_id, username)
- Проверка диапазонов (баллы, длительность)

### 11.4 SQL инъекции
- Все запросы через SQLAlchemy ORM
- Параметризованные запросы
- Нет прямой конкатенации SQL

---

## 12. СЦЕНАРИИ ТЕСТИРОВАНИЯ

### 12.1 Сценарий 1: Полный цикл работы с учеником

1. **Вход в систему** (`/login`)
   - Ввести корректные данные
   - Проверить редирект на `/dashboard`

2. **Создание ученика** (`/student/new`)
   - Заполнить все обязательные поля
   - Выбрать категорию "ЕГЭ"
   - Указать целевой балл 85
   - Сохранить
   - Проверить редирект на профиль ученика

3. **Создание урока** (`/student/<id>/lesson/new`)
   - Выбрать тип "Обычный урок"
   - Указать дату и время (будущее)
   - Указать длительность 60 минут
   - Сохранить
   - Проверить появление урока в профиле

4. **Генерация ДЗ** (`/kege-generator/<lesson_id>?assignment_type=homework`)
   - Выбрать тип задания (например, Задание 1)
   - Указать количество 5
   - Сгенерировать
   - Проверить отображение результатов

5. **Принятие заданий** (`/results`)
   - Принять все задания
   - Проверить обновление статуса ДЗ урока
   - Проверить появление заданий в профиле урока

6. **Проверка ДЗ** (`/lesson/<lesson_id>/homework`)
   - Просмотреть все задания
   - Проверить корректность отображения

### 12.2 Сценарий 2: Работа с пропущенными заданиями

1. **Генерация заданий** (`/kege-generator`)
   - Сгенерировать набор заданий

2. **Пропуск заданий** (`/results`)
   - Пропустить несколько заданий
   - Проверить их появление в `/skipped`

3. **Возврат в выборку** (`/skipped`)
   - Вернуть одно задание в выборку
   - Проверить его исчезновение из списка пропущенных

4. **Повторная генерация** (`/kege-generator`)
   - Включить опцию "Включить пропущенные"
   - Сгенерировать задания
   - Проверить появление ранее пропущенных заданий

### 12.3 Сценарий 3: Массовые операции

1. **Генерация большого набора** (`/kege-generator`)
   - Сгенерировать 20 заданий

2. **Массовое принятие** (`/results`)
   - Выбрать все задания (чекбокс "Выбрать все")
   - Нажать "Принять все"
   - Проверить применение действия ко всем заданиям

3. **Массовое добавление в черный список** (`/results`)
   - Выбрать несколько заданий
   - Добавить в черный список
   - Проверить подтверждение (модальное окно)
   - Проверить, что задания больше не появляются

### 12.4 Сценарий 4: Расписание

1. **Просмотр расписания** (`/schedule`)
   - Проверить отображение всех уроков
   - Проверить фильтрацию по датам

2. **Создание урока из расписания** (`/schedule/create-lesson`)
   - Выбрать дату в календаре
   - Создать урок
   - Проверить появление в календаре

3. **Начало урока** (из профиля ученика)
   - Нажать "Начать урок"
   - Проверить изменение статуса на "Идет сейчас"

4. **Завершение урока**
   - Изменить статус на "Проведен"
   - Проверить сохранение

### 12.5 Сценарий 5: Ошибки и валидация

1. **Вход с неверными данными** (`/login`)
   - Ввести несуществующий логин
   - Проверить сообщение об ошибке

2. **Создание ученика с ошибками** (`/student/new`)
   - Оставить имя пустым
   - Проверить валидацию
   - Указать неверный диапазон баллов (например, 150)
   - Проверить валидацию

3. **Создание урока с прошедшей датой** (`/student/<id>/lesson/new`)
   - Указать дату в прошлом
   - Проверить валидацию (если реализована)

4. **Генерация без выбора типа** (`/kege-generator`)
   - Не выбрать тип задания
   - Попытаться сгенерировать
   - Проверить валидацию

---

## 13. ОГРАНИЧЕНИЯ ДЛЯ ТЕСТИРОВЩИКОВ

### 13.1 Недоступные функции

**Роль `tester` НЕ имеет доступа к:**
- `/admin` - Админ-панель
- `/admin-audit` - Логи аудита
- `/admin-testers` - Управление пользователями
- Создание/редактирование/удаление пользователей
- Просмотр системных логов

### 13.2 Доступные функции

**Роль `tester` имеет полный доступ к:**
- Управление учениками (CRUD)
- Управление уроками (CRUD)
- Генератор заданий
- Расписание
- Планы
- Импорт/экспорт данных
- Профиль пользователя

---

## 14. ЧЕКЛИСТ ТЕСТИРОВАНИЯ

### 14.1 Авторизация
- [ ] Вход с корректными данными
- [ ] Вход с неверными данными
- [ ] Выход из системы
- [ ] Доступ к защищенным страницам без авторизации
- [ ] Сохранение сессии после перезагрузки страницы

### 14.2 Управление учениками
- [ ] Создание ученика
- [ ] Редактирование ученика
- [ ] Просмотр профиля ученика
- [ ] Удаление ученика
- [ ] Валидация полей формы
- [ ] Уникальность platform_id

### 14.3 Управление уроками
- [ ] Создание урока
- [ ] Редактирование урока
- [ ] Начало урока
- [ ] Завершение урока
- [ ] Удаление урока
- [ ] Управление ДЗ
- [ ] Валидация даты и времени

### 14.4 Генератор заданий
- [ ] Генерация общего набора заданий
- [ ] Генерация ДЗ для урока
- [ ] Генерация классной работы
- [ ] Генерация проверочной работы
- [ ] Принятие заданий
- [ ] Пропуск заданий
- [ ] Добавление в черный список
- [ ] Массовые операции
- [ ] Включение пропущенных заданий

### 14.5 Расписание
- [ ] Просмотр календаря
- [ ] Фильтрация по датам
- [ ] Фильтрация по ученикам
- [ ] Создание урока из расписания

### 14.6 Пропущенные задания
- [ ] Просмотр списка пропущенных
- [ ] Возврат в выборку
- [ ] Принятие из списка пропущенных
- [ ] Добавление в черный список из пропущенных

### 14.7 Принятые задания
- [ ] Просмотр списка принятых
- [ ] Фильтрация по типу задания
- [ ] Сброс истории использования

### 14.8 UI/UX
- [ ] Навигация работает на всех страницах
- [ ] Адаптивность (мобильные устройства)
- [ ] Flash-сообщения отображаются
- [ ] Модальные окна работают
- [ ] Подтверждения удаления работают
- [ ] Поиск работает

### 14.9 Производительность
- [ ] Генерация большого количества заданий (50+)
- [ ] Загрузка страницы с большим количеством учеников
- [ ] Загрузка страницы с большим количеством уроков

### 14.10 Безопасность
- [ ] CSRF токены присутствуют во всех формах
- [ ] Невозможность доступа к админ-панели для tester
- [ ] Валидация всех пользовательских данных
- [ ] Защита от SQL инъекций (проверка через логи)

---

## 15. ИЗВЕСТНЫЕ ОСОБЕННОСТИ

### 15.1 Часовые пояса
- Платформа поддерживает два часовых пояса: МСК и ТОМСК
- При создании урока выбирается часовой пояс
- Все даты и времена сохраняются с учетом часового пояса

### 15.2 Автоматическое обновление статусов
- Уроки автоматически переходят в статус "Проведен" после окончания времени
- Статус ДЗ автоматически обновляется при назначении заданий

### 15.3 Каскадное удаление
- При удалении ученика удаляются все его уроки
- При удалении урока удаляются все связанные задания (LessonTask)

### 15.4 Уникальность заданий
- Генератор гарантирует уникальность (не показывает использованные задания)
- Исключение: пропущенные задания можно вернуть в выборку
- Черный список: задания никогда не показываются

---

## 16. КОНТАКТЫ И ПОДДЕРЖКА

При обнаружении ошибок или несоответствий в поведении системы:
1. Зафиксировать шаги воспроизведения
2. Сделать скриншоты
3. Проверить консоль браузера (F12) на наличие ошибок
4. Проверить логи сервера
5. Сообщить разработчику с описанием проблемы

---

**Конец документа**


