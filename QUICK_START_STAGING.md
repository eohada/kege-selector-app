# 🚀 Быстрый старт: Staging на Render

## Что было сделано

✅ Создана конфигурация для деплоя на Render  
✅ Добавлена поддержка PostgreSQL (автоматическое переключение с SQLite)  
✅ Создан скрипт для копирования данных из локальной БД в staging  
✅ Обновлены зависимости (gunicorn, psycopg2-binary)

## 📝 Следующие шаги

### 1. Закоммить изменения

```bash
git add .
git commit -m "Add Render staging deployment"
git push origin main
```

### 2. Создать сервисы на Render

1. Зайди на [dashboard.render.com](https://dashboard.render.com)
2. **New +** → **Blueprint**
3. Подключи GitHub репозиторий
4. Render автоматически создаст Web Service и PostgreSQL Database из `render.yaml`

### 3. Инициализировать БД

После деплоя открой **Shell** в Render Dashboard и выполни:

```bash
python scripts/init_staging_db.py
```

Скрипт скопирует все данные из твоей локальной SQLite БД в staging PostgreSQL.

### 4. Готово! 🎉

Staging будет доступен по адресу: `https://kege-selector-staging.onrender.com`

## ⚠️ Важные моменты

- **Free план**: Web Service засыпает после 15 минут неактивности (первый запрос будет медленным)
- **Free план**: PostgreSQL доступен 90 дней, затем нужно будет обновить
- **Изоляция**: Все изменения в staging не влияют на твой локальный сайт
- **Обновление данных**: Запускай `init_staging_db.py` когда нужно обновить staging данными

## 📚 Подробная инструкция

Смотри `DEPLOY_STAGING.md` для детальной информации.

